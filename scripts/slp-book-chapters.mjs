export const BOOK = {
  title: "Speech and Language Processing",
  shortTitle: "Speech & Language Processing",
  authors: "Daniel Jurafsky and James H. Martin",
  edition: "3rd edition draft",
  release: "January 6, 2026",
  source: "https://web.stanford.edu/~jurafsky/slp3/",
};

export const CHAPTERS = [
  {
    n: 1,
    slug: "introduction",
    title: "Introduction",
    focus: "The map of language technology: what NLP and speech systems do, why language is difficult, and how the book connects models, data, linguistic structure, and evaluation.",
    oneMinute: "Language technology turns speech or text into useful decisions and turns internal representations back into language. The hard part is not merely recognizing words. Meaning depends on context, world knowledge, social conventions, speaker goals, and ambiguity at every level. Modern systems learn statistical patterns from data, but successful engineering still needs explicit task definitions, representative datasets, careful evaluation, and an understanding of where models fail. This chapter is the orientation map for the entire book: begin with tokens and probabilities, build neural and transformer models, adapt them to tasks, then recover linguistic and conversational structure.",
    concepts: [
      ["Language is structured ambiguity", "A sentence can have several tokenizations, syntactic analyses, referents, intents, or emotional readings. Humans resolve these alternatives using context almost invisibly. A language system must represent uncertainty instead of assuming the first plausible interpretation is correct. The useful engineering question is therefore not “what does this string mean?” but “which interpretation is most supported by the available evidence and the task?”"],
      ["Four recurring ingredients", "Most systems combine a representation of the input, a model that assigns scores or probabilities, data that teaches or constrains the model, and an evaluation procedure. These ingredients recur whether the task is sentiment classification, translation, speech recognition, retrieval, or dialogue. Changing one without checking the others commonly creates a misleading improvement."],
      ["Understanding versus useful behavior", "A system may generate fluent text without a stable model of truth, or classify an utterance correctly for a spurious reason. Behavioral success on a benchmark is evidence about a defined task, not proof of human-like understanding. This distinction becomes essential when models are deployed in safety-critical, multilingual, or socially sensitive settings."],
      ["The pipeline has become a stack", "Older NLP diagrams showed hand-built stages such as tokenize, tag, parse, and extract. Modern foundation models can learn several stages jointly, but the underlying questions remain. Inputs still need tokenization; outputs still need decoding; claims still need grounding; entities and discourse still matter. End-to-end learning moves boundaries—it does not make the boundaries irrelevant."],
      ["Evaluation is part of the specification", "Accuracy, F1, perplexity, BLEU, word error rate, retrieval recall, and human preference measure different things. A metric only becomes meaningful after the population, unit of analysis, error costs, and deployment conditions are named. The book repeatedly returns to this lesson because language quality cannot be compressed safely into one universal number."],
    ],
    worked: {
      title: "A support assistant from input to decision",
      text: "Suppose a user says, “It charged me twice, can you undo the second one?” Tokenization creates model inputs. A language model represents the utterance in context. Retrieval finds the payment policy and account events. Entity and coreference processing connect “it” and “the second one” to transactions. A classifier or generated plan identifies the refund intent. A dialogue policy asks for confirmation if required. Evaluation must test not only fluent replies but correct transaction selection, policy compliance, latency, refusal behavior, and robustness to incomplete context. One sentence touches nearly every layer introduced in the book.",
    },
    pitfalls: [
      "Treating fluent output as evidence that the answer is grounded or correct.",
      "Using a benchmark whose examples do not resemble the real users, languages, or error costs.",
      "Hiding uncertainty behind a single top prediction instead of inspecting alternatives and confidence.",
      "Calling a system “end to end” and then ignoring tokenization, retrieval, decoding, or post-processing choices.",
      "Optimizing average quality while missing rare but expensive failures.",
    ],
    practice: [
      "Choose one language product and write its input, output, user, and failure cost in one sentence each.",
      "Draw the system as representation → model → data → evaluation.",
      "List three ambiguities the system must resolve and what evidence could resolve each one.",
      "Define one automatic metric and one human or task-level check.",
      "Create five adversarial examples that differ from the happy path.",
    ],
    faq: [
      ["Is NLP only about text?", "No. The field includes speech recognition, speech synthesis, spoken dialogue, and models that connect language with other modalities. The book deliberately treats speech and text as related parts of one discipline."],
      ["Do large language models make linguistics obsolete?", "No. They reduce the amount of structure engineers must hand-code, but linguistic categories remain useful for analysis, evaluation, data design, interpretability, and diagnosing systematic errors."],
      ["Why are there so many metrics?", "Each task exposes a different behavior and different error costs. A metric is a lens, not a universal definition of quality."],
    ],
    takeaways: [
      "Language processing is inference under ambiguity.",
      "Representations, models, data, and evaluation form the recurring system skeleton.",
      "Fluency, correctness, grounding, and usefulness are different properties.",
      "Modern end-to-end models still contain important engineering boundaries.",
      "Evaluation choices define what “better” means.",
    ],
  },
  {
    n: 2,
    slug: "words-and-tokens",
    title: "Words and Tokens",
    focus: "Words, morphemes, Unicode, subword tokenization, corpora, regular expressions, rule-based tokenization, and minimum edit distance.",
    oneMinute: "Before a model can process language, raw characters must become stable units. Spaces are not a universal definition of a word, Unicode characters are not the same as bytes, and a fixed word vocabulary breaks on names, spelling variants, and morphologically rich languages. Modern systems therefore use subword tokens, commonly learned with Byte-Pair Encoding, while preserving an exact path back to the original text. Regular expressions and rule-based tokenizers remain essential for cleaning and boundary decisions. Minimum edit distance supplies a reusable dynamic-programming measure for spelling, alignment, and speech-recognition errors.",
    concepts: [
      ["A word is a task-dependent decision", "Punctuation, contractions, filled pauses, multiword names, and languages without written spaces make “split on whitespace” unreliable. Word types count distinct forms; word instances count occurrences. The right boundary depends on what the downstream task must preserve."],
      ["Morphemes explain why words grow", "A morpheme is a minimal meaning-bearing unit, such as a stem or affix. Languages combine them differently: isolating languages often use short independent units, while agglutinative languages can pack many functions into one surface word. Subword models help share statistical strength across related forms without requiring a perfect linguistic analyzer."],
      ["Unicode is the character contract", "Unicode assigns code points; encodings such as UTF-8 turn those code points into bytes. A visible symbol may contain multiple code points, and visually similar text may have different underlying forms. Normalization, script handling, and preservation of byte offsets are therefore correctness concerns, not cosmetic cleanup."],
      ["BPE learns a practical vocabulary", "Byte-Pair Encoding begins with small units and repeatedly merges frequent adjacent pairs. The result balances vocabulary size against sequence length: common strings become single tokens, while rare words remain representable as pieces. The learned merges are data-dependent, so language coverage and corpus quality directly affect token efficiency."],
      ["Edit distance aligns two sequences", "Levenshtein distance finds the cheapest sequence of insertions, deletions, and substitutions using a dynamic-programming table. Backtracing reveals the alignment, not just the score. That makes the algorithm useful for spelling correction, morphology, DNA-like sequence comparison, and word error rate in speech recognition."],
    ],
    worked: {
      title: "Tokenizing a mixed, messy sentence",
      text: "Consider “Shivam’s résumé costs ₹2,500—okay?” A robust pipeline preserves the curly apostrophe and accented character through Unicode normalization, decides whether “Shivam’s” is one surface unit or two grammatical units, prevents the thousands separator from splitting the number incorrectly, and treats the dash and question mark according to the model’s vocabulary. A byte-level fallback guarantees that no symbol becomes unknown. The output tokens are model units, not claims about the one true set of words.",
    },
    pitfalls: [
      "Assuming one Unicode code point always equals one visible character.",
      "Training a tokenizer on one language mix and using token counts as a fair cost comparison across all languages.",
      "Normalizing away case, accents, punctuation, or whitespace that carries task-relevant information.",
      "Letting preprocessing in training differ from preprocessing in production.",
      "Reporting edit distance without showing how insertions, deletions, and substitutions were weighted.",
    ],
    practice: [
      "Inspect code points and UTF-8 bytes for five multilingual strings.",
      "Train a tiny BPE vocabulary and watch which pairs merge first.",
      "Compare whitespace, rule-based, and subword tokenization on URLs, emoji, and contractions.",
      "Implement the edit-distance table and backtrace.",
      "Measure tokens per word across at least three languages in the same tokenizer.",
    ],
    faq: [
      ["Why not tokenize everything as characters?", "Characters avoid unknown words but produce long sequences and do not map cleanly to meaning in every writing system. Subwords are a useful compromise."],
      ["Is BPE linguistically correct?", "Not necessarily. It learns frequent strings, not guaranteed morphemes. Its goal is efficient, lossless model input rather than a perfect theory of word structure."],
      ["When are regexes still useful?", "They are excellent for precise local patterns, validation, cleanup, and rule-based boundaries. They are not a general solution for meaning or long-range context."],
    ],
    takeaways: [
      "Token boundaries are choices made for a task.",
      "Unicode code points, graphemes, and bytes are different levels.",
      "Subword vocabularies trade sequence length for vocabulary size.",
      "Tokenizer training data affects efficiency and fairness.",
      "Edit distance is both a score and an alignment algorithm.",
    ],
  },
  {
    n: 3,
    slug: "n-gram-language-models",
    title: "N-gram Language Models",
    focus: "Conditional probability, n-grams, training and test splits, perplexity, sampling, overfitting, smoothing, interpolation, backoff, and entropy.",
    oneMinute: "A language model assigns probabilities to sequences. An n-gram model makes this tractable by predicting the next word from only the previous n−1 words. Counts give maximum-likelihood probabilities, but real language produces unseen combinations, so smoothing must reserve probability mass for them. Interpolation combines several context lengths; backoff uses shorter contexts when longer ones are unreliable. Perplexity measures how surprised the model is by held-out text, but it only compares models with the same tokenization and evaluation conditions. N-grams are simple, fast, interpretable, and still valuable as baselines even when neural models dominate.",
    concepts: [
      ["The chain rule becomes a Markov approximation", "The probability of a sentence can be decomposed into next-token conditional probabilities. Conditioning on the entire history is impossible with sparse data, so an n-gram model keeps a short suffix. A bigram sees one previous token; a trigram sees two. More context can help until sparse counts overwhelm the benefit."],
      ["Counts estimate probabilities", "Maximum-likelihood estimation divides an n-gram count by the count of its prefix. Sentence-start and sentence-end symbols make boundaries learnable. The model is normalized locally: the next-token probabilities after a history must sum to one."],
      ["Unseen events need probability mass", "A zero probability for one unseen n-gram makes the whole sentence probability zero. Smoothing discounts seen events and reallocates mass to unseen ones. Add-one smoothing is easy to explain but usually distorts probabilities; interpolation and Kneser–Ney-style ideas are more useful in practice."],
      ["Perplexity is average surprise", "Perplexity is the exponentiated average negative log probability per token. Lower is better on matched held-out data. It is an intrinsic measure: it tells us how well the model predicts the corpus, not automatically whether a downstream application improves."],
      ["Sampling exposes model behavior", "Generate by repeatedly drawing a token from the conditional distribution and appending it to the history. Samples reveal memorization, local fluency, and broken long-range structure more intuitively than a single perplexity number."],
    ],
    worked: {
      title: "A tiny restaurant bigram model",
      text: "From sentences such as “I want tea” and “I want coffee,” count transitions including start and end symbols. The history “want” may assign probability 1/2 to “tea” and 1/2 to “coffee.” If test text contains “want dosa,” the unsmoothed probability is zero even though the sentence is reasonable. Interpolating a bigram estimate with a unigram estimate gives “dosa” some chance based on its overall frequency. The example shows why local counts are useful and why unseen language cannot be ignored.",
    },
    pitfalls: [
      "Evaluating on training text and mistaking memorization for generalization.",
      "Comparing perplexities produced by different tokenizers or vocabularies.",
      "Using add-one smoothing as a strong production method because it is easy to implement.",
      "Increasing n without checking data sparsity and memory cost.",
      "Assuming lower perplexity guarantees better task outcomes or safer generations.",
    ],
    practice: [
      "Build unigram, bigram, and trigram counts with explicit boundary symbols.",
      "Verify that next-token probabilities sum to one for several histories.",
      "Compare unsmoothed, add-k, and interpolated models on held-out text.",
      "Generate samples at different context lengths.",
      "Inspect the n-grams contributing the largest test-set loss.",
    ],
    faq: [
      ["Why study n-grams in the LLM era?", "They make probability, smoothing, evaluation, and data leakage concrete. They are also fast baselines and useful in constrained or resource-limited systems."],
      ["Can perplexity compare any two language models?", "Only when tokenization, test data, and probability accounting are compatible. Otherwise the per-token units differ."],
      ["What is backoff?", "Use the longest reliable context first, then fall back to shorter histories when the long n-gram is unseen or weakly supported."],
    ],
    takeaways: [
      "Language modeling is next-token probability estimation.",
      "N-grams approximate long histories with short contexts.",
      "Smoothing prevents unseen events from receiving impossible zero probability.",
      "Perplexity measures prediction under a fixed evaluation setup.",
      "Simple probabilistic baselines remain diagnostically valuable.",
    ],
  },
  {
    n: 4,
    slug: "logistic-regression-text-classification",
    title: "Logistic Regression and Text Classification",
    focus: "Supervised classification, the sigmoid and softmax, features, cross-entropy, gradient descent, precision, recall, F1, cross-validation, and significance testing.",
    oneMinute: "Text classification maps an input to a label such as spam, sentiment, or intent. Logistic regression turns a weighted sum of features into a probability: sigmoid for binary decisions and softmax for multiple classes. Training minimizes cross-entropy with gradient-based optimization, usually with regularization to limit overfitting. The model is linear but powerful when features are informative, and its weights are inspectable. Evaluation must look beyond accuracy when classes or error costs are uneven; precision, recall, F1, confusion matrices, held-out test sets, and statistical testing reveal different failure modes.",
    concepts: [
      ["Features make text numeric", "A document can be represented by word counts, binary indicators, normalized frequencies, character n-grams, metadata, or learned embeddings. The feature design determines what evidence the classifier can use. Sparse bag-of-words features discard order but often form a strong baseline."],
      ["Sigmoid and softmax create probabilities", "A linear score can range from negative to positive infinity. The sigmoid maps a binary score into 0–1; softmax normalizes several class scores into a distribution. Decision thresholds can be changed after training to match different costs."],
      ["Cross-entropy rewards calibrated confidence", "Cross-entropy penalizes the negative log probability assigned to the correct label. A confidently wrong prediction costs much more than an uncertain one. Minimizing this differentiable loss connects probabilistic modeling to gradient descent."],
      ["Regularization controls brittle weights", "L2 regularization discourages very large weights; L1 can encourage sparsity. Both trade a small amount of training fit for better generalization. The regularization strength is a hyperparameter chosen without looking at the final test set."],
      ["Metrics encode operational priorities", "Precision asks how many predicted positives were correct; recall asks how many real positives were found. F1 balances them but hides the chosen threshold and class distribution. A confusion matrix shows which labels are being exchanged."],
    ],
    worked: {
      title: "Urgent-ticket classification",
      text: "Represent each support message with unigram and bigram counts. A learned positive weight for “service down” and a negative weight for “feature request” contribute to an urgency score. Sigmoid converts the score to a probability. If missing an outage is far more costly than reviewing a false alarm, lower the decision threshold and accept reduced precision for higher recall. Evaluate on tickets from a later time period so repeated templates do not leak across the split.",
    },
    pitfalls: [
      "Using accuracy on a dataset where the majority class dominates.",
      "Tuning features or thresholds repeatedly on the test set.",
      "Allowing duplicates, users, or time-adjacent examples to leak across splits.",
      "Reading a large coefficient as causal evidence.",
      "Publishing one score without uncertainty, per-class results, or a confusion matrix.",
    ],
    practice: [
      "Train a bag-of-words baseline before a larger neural model.",
      "Plot precision and recall across thresholds.",
      "Inspect the largest positive and negative weights for each class.",
      "Compare random, grouped, and time-based splits.",
      "Bootstrap the test set to estimate a confidence interval.",
    ],
    faq: [
      ["Why use logistic regression when transformers exist?", "It is fast, data-efficient, interpretable, and a strong baseline. If a complex model cannot beat it reliably, complexity has not earned its cost."],
      ["Does a 0.8 output mean 80% certainty?", "Only if probabilities are calibrated on data like deployment data. Calibration should be measured rather than assumed."],
      ["When should I prefer recall?", "When false negatives are more costly—for example missing abuse, outages, or critical medical mentions. The threshold should follow the cost model."],
    ],
    takeaways: [
      "Classification quality begins with representation and split design.",
      "Logistic regression is a probabilistic linear model.",
      "Cross-entropy and regularization shape learning.",
      "Thresholds belong to the product decision, not just the model.",
      "Precision, recall, F1, and confusion matrices answer different questions.",
    ],
  },
  {
    n: 5,
    slug: "embeddings",
    title: "Embeddings",
    focus: "Lexical semantics, distributional meaning, count vectors, cosine similarity, word2vec, semantic properties, bias, and evaluation.",
    oneMinute: "An embedding represents a word or other item as a dense vector. Its central intuition is distributional: expressions appearing in similar contexts tend to have related meanings. Count-based vectors make the evidence explicit; word2vec-style objectives learn compact vectors by predicting context. Cosine similarity compares direction rather than magnitude. Embeddings capture useful similarity and analogy patterns, but they also compress corpus stereotypes and can confuse association with meaning. Evaluation therefore needs task relevance, neighborhood inspection, and bias analysis—not only a list of impressive nearest neighbors.",
    concepts: [
      ["Meaning from context", "A word is represented by the contexts in which it occurs. “Coffee” and “tea” receive similar vectors because their neighboring words and grammatical roles overlap. This captures graded similarity without requiring a hand-built dictionary, though it reflects the corpus rather than a complete theory of meaning."],
      ["Sparse counts versus dense vectors", "A term-context matrix has interpretable dimensions but can be huge and sparse. Weighting schemes such as PPMI emphasize informative co-occurrences. Predictive models learn fewer dense dimensions that are harder to name individually but more convenient for downstream learning."],
      ["Cosine compares orientation", "Cosine similarity divides the dot product by vector lengths. It focuses on pattern rather than raw frequency magnitude. The choice is useful but not neutral: different normalization, context windows, and distance measures produce different neighborhoods."],
      ["Word2vec learns a prediction space", "Skip-gram predicts nearby words from a center word; negative sampling contrasts observed pairs with sampled non-pairs. Training nudges related vectors together and unrelated samples apart. Window size changes what “similar” means—small windows emphasize functional similarity; broad windows often emphasize topic."],
      ["Bias lives in geometry", "If social groups are associated unevenly with occupations, emotions, or evaluations in the training corpus, vector neighborhoods reproduce those associations. Removing one direction rarely removes all downstream harm. Dataset documentation and task-specific impact testing are essential."],
    ],
    worked: {
      title: "Why “doctor” and “nurse” become neighbors",
      text: "Collect context counts around each word. Both may co-occur with “hospital,” “patient,” “care,” and “treatment,” so their context vectors point in similar directions. PPMI reduces the dominance of extremely frequent words. A dense model compresses these patterns into fewer dimensions. The resulting similarity is useful for retrieval, but it does not say the roles are interchangeable, and demographic associations in the same corpus may create harmful occupational stereotypes.",
    },
    pitfalls: [
      "Interpreting cosine similarity as equivalence or factual truth.",
      "Ignoring frequency, domain, and context-window effects.",
      "Evaluating only on analogy sets that reward cultural or dataset artifacts.",
      "Treating a static vector as if a word has one meaning in every sentence.",
      "Assuming a single debiasing transformation removes downstream bias.",
    ],
    practice: [
      "Build a small term-context matrix and apply PPMI.",
      "Compare nearest neighbors under small and large context windows.",
      "Inspect neighborhoods for polysemous words.",
      "Run a task-specific association or bias probe.",
      "Compare static embeddings with contextual embeddings from Chapter 10.",
    ],
    faq: [
      ["What does one embedding dimension mean?", "Usually nothing simple. Meaning is distributed across dimensions, especially in learned dense spaces."],
      ["Why cosine instead of Euclidean distance?", "Cosine ignores overall vector magnitude and often better reflects contextual pattern, but the best measure depends on training and use."],
      ["Are embeddings a dictionary?", "No. They encode statistical associations from a corpus, not clean symbolic definitions or guaranteed facts."],
    ],
    takeaways: [
      "Distributional context creates a learnable geometry of association.",
      "Count and predictive embeddings expose different tradeoffs.",
      "Cosine similarity is useful but not a semantic proof.",
      "Training choices determine the neighborhood structure.",
      "Bias analysis is part of embedding evaluation.",
    ],
  },
  {
    n: 6,
    slug: "neural-networks",
    title: "Neural Networks",
    focus: "Units, nonlinear activation, XOR, feedforward networks, classification, embedding inputs, backpropagation, optimization, and regularization.",
    oneMinute: "A neural network composes linear transformations with nonlinear activation functions. A single linear unit can only draw one flat decision boundary; hidden layers solve problems such as XOR by creating intermediate features. In NLP, embeddings become input vectors, feedforward layers transform them, and an output head produces class probabilities or scores. Backpropagation uses the chain rule to compute gradients efficiently. Training quality depends on initialization, optimization, batching, regularization, and validation—not just architecture size.",
    concepts: [
      ["A unit is weighted evidence plus activation", "Inputs are multiplied by weights, summed with a bias, and passed through a function such as ReLU, sigmoid, or tanh. The activation introduces nonlinearity. Without it, stacking many layers would collapse into one linear transformation."],
      ["Hidden layers learn features", "The XOR example proves why intermediate representations matter: no single line separates the classes, but two hidden units can carve the space into regions that a final unit combines. NLP networks similarly learn useful combinations of word or feature signals."],
      ["Forward pass and loss", "The forward pass computes predictions layer by layer. A task-appropriate loss compares predictions with targets—for example cross-entropy for classification. The loss is a training objective, not automatically the deployment metric."],
      ["Backpropagation is organized chain rule", "Starting from the loss, backpropagation reuses intermediate derivatives to assign credit or blame to every parameter. An optimizer such as stochastic gradient descent or Adam updates parameters using minibatch gradients."],
      ["Generalization needs controls", "Dropout, weight decay, early stopping, data augmentation, and careful validation help prevent memorization. Learning rate is often the most important hyperparameter: too high diverges; too low wastes compute or stalls."],
    ],
    worked: {
      title: "A feedforward sentiment classifier",
      text: "Average or concatenate embeddings from a short review, multiply by a hidden-layer matrix, apply ReLU, and send the hidden vector to a softmax output for positive, neutral, or negative. Cross-entropy measures the error. Backpropagation updates the classifier and, if allowed, the embeddings. Inspect learning curves: falling training loss with rising validation loss indicates overfitting, not continued progress.",
    },
    pitfalls: [
      "Stacking linear layers without nonlinear activations.",
      "Using the final test set to choose epochs or hyperparameters.",
      "Assuming a lower training loss means better generalization.",
      "Ignoring exploding, vanishing, or numerically unstable gradients.",
      "Changing architecture before checking learning rate, preprocessing, and labels.",
    ],
    practice: [
      "Implement XOR with and without a hidden nonlinear layer.",
      "Run a forward pass by hand on a two-feature network.",
      "Use automatic differentiation, then verify one gradient numerically.",
      "Plot training and validation loss across epochs.",
      "Ablate dropout or weight decay and compare generalization.",
    ],
    faq: [
      ["Why is nonlinearity necessary?", "A composition of linear maps is still linear. Activations let the network form curved or piecewise decision boundaries and hierarchical features."],
      ["What does backpropagation learn?", "It computes gradients. The optimizer uses those gradients to update parameters; data and the loss determine the behavior being rewarded."],
      ["Is a deeper network always better?", "No. Depth can increase representational power but also optimization difficulty, cost, and overfitting. It must earn its complexity on held-out data."],
    ],
    takeaways: [
      "Neural networks are layered differentiable function approximators.",
      "Nonlinearity makes hidden representations useful.",
      "Loss, backpropagation, and optimization are separate pieces.",
      "Embeddings connect discrete language units to continuous networks.",
      "Validation curves are essential evidence about generalization.",
    ],
  },
  {
    n: 7,
    slug: "large-language-models",
    title: "Large Language Models",
    focus: "Language-model architectures, conditional generation, prompting, decoding, pretraining, scaling, evaluation, ethical risk, and safety.",
    oneMinute: "A large language model predicts tokens using a high-capacity neural architecture trained on broad corpora. Decoder-only models generate continuations, encoder-only models build bidirectional representations, and encoder-decoder models map one sequence to another. Prompting conditions behavior through text rather than parameter updates. Decoding choices—greedy selection, sampling, temperature, top-k, or nucleus sampling—change outputs even when model weights stay fixed. Pretraining produces general capabilities, but evaluation must separate predictive loss, task performance, factuality, safety, bias, and real user outcomes. Scale improves many behaviors while increasing cost and the consequences of data and objective mistakes.",
    concepts: [
      ["Three architectural roles", "Autoregressive decoders predict the next token from the left context. Bidirectional encoders represent a whole input for understanding tasks. Encoder-decoder models condition generation on a separate source sequence. Modern products may combine these roles with retrieval and tools."],
      ["Prompting is conditional computation", "Instructions, examples, delimiters, and retrieved context alter the probability distribution over continuations. Prompt design can reveal capabilities but is sensitive to wording, ordering, and context contamination. A prompt is part of the program and should be versioned and tested."],
      ["Decoding is a policy choice", "Greedy decoding is deterministic but can be bland or trapped. Temperature rescales logits; top-k limits choices to a fixed number; nucleus sampling retains a dynamic probability mass. Decoding affects diversity, repetition, factual risk, and reproducibility."],
      ["Pretraining learns by compression-like prediction", "The model processes enormous token streams and adjusts parameters to reduce next-token error. Dataset mixture, deduplication, filtering, sequence packing, compute budget, and scaling laws shape the result. Broad capability emerges from the task and data but does not guarantee grounded knowledge."],
      ["Evaluation is multidimensional", "Perplexity, benchmark scores, human preference, calibration, factuality, robustness, toxicity, privacy leakage, and downstream task success measure different properties. Contamination can turn memorization into an apparently strong score."],
    ],
    worked: {
      title: "The same model, three decoding behaviors",
      text: "Prompt a model to propose a production incident hypothesis. Greedy decoding may return the most conventional cause every time. Raising temperature produces more varied hypotheses but also more unsupported claims. Nucleus sampling can preserve diversity while excluding the least likely tail. For an incident assistant, the safest design may use low-temperature structured generation, retrieved telemetry, explicit citations, and a verifier rather than asking decoding alone to create reliability.",
    },
    pitfalls: [
      "Equating a confident, fluent continuation with a grounded answer.",
      "Changing prompts or decoding settings without treating them as model-version changes.",
      "Using contaminated benchmarks or examples memorized from training data.",
      "Reporting a single average while hiding subgroup, language, or long-context failures.",
      "Assuming scale removes bias, privacy, or misuse risk.",
    ],
    practice: [
      "Compare greedy, temperature, top-k, and nucleus decoding on one prompt set.",
      "Version a prompt template and create regression cases.",
      "Separate closed-book factuality from retrieval-grounded answering.",
      "Build an evaluation matrix covering quality, safety, latency, and cost.",
      "Inspect failures across languages and input lengths.",
    ],
    faq: [
      ["Does an LLM store a database of sentences?", "Its parameters encode statistical patterns rather than a normal searchable database, though models can memorize and reproduce some training sequences."],
      ["What is temperature?", "A scaling factor applied to logits before sampling. Lower values sharpen the distribution; higher values flatten it and increase randomness."],
      ["Why can a lower-perplexity model still be worse for my product?", "Perplexity measures token prediction on a corpus. Product success may depend on grounding, format, safety, latency, or a specialized domain."],
    ],
    takeaways: [
      "Architecture determines what context a model conditions on.",
      "Prompts and decoding settings are executable system components.",
      "Pretraining creates general statistical capability, not guaranteed truth.",
      "Scaling changes capability, cost, and risk together.",
      "LLM evaluation requires a portfolio of measures.",
    ],
  },
  {
    n: 8,
    slug: "transformers",
    title: "Transformers",
    focus: "Self-attention, transformer blocks, parallel computation, token and positional embeddings, language-model heads, sampling, training, scaling, and interpretation.",
    oneMinute: "The transformer replaces recurrence with attention: each token builds a query, key, and value and mixes information from relevant positions. Multi-head attention learns several relation patterns in parallel. A transformer block combines attention, a position-wise feedforward network, residual connections, layer normalization, and masking. Token embeddings carry identity; positional information restores order. Matrix operations make training highly parallel, while autoregressive inference still generates step by step and uses a key-value cache. The architecture is powerful but attention cost, context limits, and interpretability remain real engineering concerns.",
    concepts: [
      ["Attention is content-addressed mixing", "A query from one token is compared with keys from other tokens. Scaled dot products become softmax weights, and the output is a weighted sum of values. Causal masking prevents a language model from seeing future tokens during training."],
      ["Multiple heads, multiple subspaces", "Each head has its own projections and can emphasize different relationships. Head outputs are concatenated and remixed. Individual heads are not guaranteed to map neatly to human linguistic categories, but the collection increases representational flexibility."],
      ["The block is more than attention", "Residual connections preserve and refine the running representation; layer normalization stabilizes optimization; the feedforward sublayer transforms each position independently. Removing these supporting components changes trainability and capacity dramatically."],
      ["Position must be represented", "Self-attention alone treats inputs as an unordered set. Learned position embeddings, sinusoidal encodings, or relative/rotary schemes inject order and influence how models extrapolate to longer contexts."],
      ["Training parallelizes; decoding does not fully", "During teacher-forced training, all positions can be processed in matrices. During generation, the next token depends on prior outputs. Key-value caching avoids recomputing earlier attention states, trading memory for speed."],
    ],
    worked: {
      title: "Resolving “it” with attention",
      text: "In “The server rejected the token because it had expired,” the representation at “it” can place high attention weight on “token” rather than “server.” Queries and keys make the association; values carry information into the updated token vector. Later layers combine this signal with grammar and semantics. An attention heatmap may reveal a useful pattern, but it is not by itself proof that this edge caused the final prediction.",
    },
    pitfalls: [
      "Describing the transformer as attention alone and ignoring residual, normalization, and feedforward paths.",
      "Treating attention weights as a complete causal explanation.",
      "Forgetting the causal mask and accidentally leaking future tokens.",
      "Assuming a declared context window means every position is used equally well.",
      "Ignoring key-value cache memory and autoregressive latency in deployment.",
    ],
    practice: [
      "Compute a tiny scaled dot-product attention example by hand.",
      "Visualize the causal mask and verify future weights are zero.",
      "Track tensor shapes through a multi-head attention block.",
      "Compare inference with and without a key-value cache.",
      "Probe behavior near and beyond the trained context length.",
    ],
    faq: [
      ["Why divide dot products by the square root of key dimension?", "Without scaling, large dimensions produce large logits that saturate softmax and make gradients poorly behaved."],
      ["Why do transformers need positional encodings?", "Attention compares content but has no inherent left-to-right order. Position signals let the same tokens in different orders mean different things."],
      ["Is attention quadratic?", "Standard full attention compares every position with every other position, giving quadratic score storage in sequence length. Many variants trade exact global attention for lower cost."],
    ],
    takeaways: [
      "Attention retrieves and mixes information by learned relevance.",
      "Transformer blocks rely on several cooperating sublayers.",
      "Position information is essential.",
      "Parallel training and sequential generation have different performance profiles.",
      "Interpretation needs more evidence than an attention map.",
    ],
  },
  {
    n: 9,
    slug: "post-training-alignment-test-time-compute",
    title: "Post-training: Instruction Tuning, Alignment, and Test-Time Compute",
    focus: "Supervised instruction tuning, preference data, reward modeling, RLHF-style optimization, direct preference optimization, alignment limits, reasoning, and test-time compute.",
    oneMinute: "Pretraining teaches a model to continue text; post-training teaches it to follow instructions and behave according to selected preferences. Supervised instruction tuning learns from prompt–response examples. Preference learning uses comparisons between candidate responses and may train a reward model followed by reinforcement learning, or optimize preferences more directly with methods such as DPO. Test-time compute improves some answers by generating, critiquing, searching, or verifying additional candidates. None of these steps creates a universal human value function: data collection, annotator variation, reward hacking, over-refusal, and evaluation design remain central.",
    concepts: [
      ["Instruction tuning changes the interface", "A curated dataset pairs diverse instructions with desired responses. Fine-tuning makes following the instruction more likely than merely continuing its surface form. Coverage, response quality, and formatting diversity matter more than raw example count alone."],
      ["Preferences are relative labels", "Annotators compare two or more responses under a rubric. A reward model can learn to score candidates, but it learns the dataset’s judgments and shortcuts. Agreement, annotator context, and prompt distribution should be documented."],
      ["RLHF and direct preference methods", "RLHF commonly optimizes a policy against a learned reward while constraining drift from a reference model. Direct preference optimization reframes preference pairs into a classification-like objective without a separate online reinforcement-learning loop. Both depend on the quality and scope of comparisons."],
      ["Alignment has competing objectives", "Helpfulness, harmlessness, honesty, style, and user autonomy can conflict. A single scalar reward compresses those tensions. Models may over-refuse safe requests, flatter users, exploit reward-model weaknesses, or perform well on familiar safety tests while failing under distribution shift."],
      ["Test-time compute is an inference budget", "A system can sample multiple solutions, ask for critique, search a reasoning tree, call tools, or run a verifier. More compute helps only when candidate diversity and selection correlate with correctness; otherwise it multiplies confident errors and cost."],
    ],
    worked: {
      title: "Training a safer troubleshooting assistant",
      text: "Begin with supervised examples that show concise diagnosis, evidence requests, and safe commands. Collect pairwise preferences where reviewers choose grounded, reversible guidance over guesses or destructive actions. Optimize the model while monitoring drift from its base capabilities. At inference time, generate two plans, run a policy and command-risk checker, and select the plan whose claims are supported by telemetry. Evaluate helpfulness and refusal separately so the model cannot “win” safety by refusing everything.",
    },
    pitfalls: [
      "Treating annotator preference as objective ground truth.",
      "Optimizing a reward score without checking for reward hacking or style shortcuts.",
      "Measuring safety only by refusal rate.",
      "Using more reasoning tokens without a reliable verifier.",
      "Ignoring preference differences across cultures, domains, and user roles.",
    ],
    practice: [
      "Write an explicit preference rubric with examples of ties and uncertainty.",
      "Measure inter-annotator agreement and inspect disagreements.",
      "Compare supervised tuning, reward-model scoring, and direct preference objectives conceptually.",
      "Test over-refusal and unsafe compliance as separate axes.",
      "Plot quality versus latency and cost for several test-time-compute budgets.",
    ],
    faq: [
      ["What is the difference between pretraining and instruction tuning?", "Pretraining predicts broad text. Instruction tuning concentrates behavior on responding to explicit tasks in a desired format."],
      ["Does DPO remove the need for preference data?", "No. It simplifies the optimization pipeline, but it still learns from chosen and rejected responses and inherits their coverage and biases."],
      ["Does chain-of-thought guarantee reasoning?", "No. Longer visible explanations can be wrong or post-hoc. Correctness needs outcome checks, tools, or verifiers when possible."],
    ],
    takeaways: [
      "Post-training converts general continuation into a more usable assistant policy.",
      "Preference labels are scoped human judgments, not universal truth.",
      "Optimization can exploit weaknesses in the reward signal.",
      "Safety and usefulness need separate, adversarial evaluation.",
      "Test-time compute is valuable when selection and verification are reliable.",
    ],
  },
  {
    n: 10,
    slug: "masked-language-models",
    title: "Masked Language Models",
    focus: "Bidirectional transformer encoders, masked-token training, contextual embeddings, fine-tuning for classification, and sequence labeling.",
    oneMinute: "Masked language models such as BERT build bidirectional representations: a token can use both left and right context. During pretraining, selected tokens are hidden or corrupted and the model predicts them. The resulting contextual embeddings change with the sentence, solving many limitations of one-vector-per-word representations. A task head can then be fine-tuned for document classification, sentence-pair tasks, or token labeling such as named entity recognition. Masked models are representation learners rather than natural left-to-right generators, and their evaluation must account for pretraining–fine-tuning mismatch and data leakage.",
    concepts: [
      ["Bidirectionality changes the available evidence", "An encoder can represent “bank” using words before and after it. Unlike a causal decoder, it is not required to hide future tokens. This makes encoders efficient for understanding a complete input."],
      ["Masked-token prediction creates supervision", "Corrupt a subset of tokens and ask the model to reconstruct them. Every corpus supplies labels without manual annotation. Masking strategy matters because the special mask symbol does not appear in ordinary downstream text."],
      ["Contextual embeddings are token-in-sentence vectors", "The same surface word receives different hidden states in different contexts. Earlier layers often preserve more local or lexical information; later layers become more task- and context-sensitive, though layer roles are not perfectly fixed."],
      ["Fine-tuning attaches a small task head", "For classification, a pooled sequence representation feeds a linear head. For sequence labeling, each token representation receives a label score. Updating the entire encoder adapts features but can overfit small datasets."],
      ["Subword labels need alignment", "Named entities may split into multiple subword tokens. Training and evaluation must decide whether to label the first piece, all pieces, or reconstruct word-level spans. Silent alignment mistakes can inflate or destroy scores."],
    ],
    worked: {
      title: "Two meanings of “port”",
      text: "In “the ship entered the port,” surrounding words push the token representation toward a harbor sense. In “the service listens on port 443,” the representation moves toward networking. A static embedding cannot make this distinction. For named entity recognition, the encoder’s per-token states can identify “Port Blair” as a location, but subword pieces must be mapped back to the original span before scoring.",
    },
    pitfalls: [
      "Using a masked encoder as if it were a normal autoregressive generator.",
      "Misaligning word-level labels with subword pieces.",
      "Fine-tuning a large encoder on a tiny dataset without regularization or multiple seeds.",
      "Comparing models whose pretraining corpora leak evaluation examples.",
      "Assuming the pooled special-token vector is always the best sequence representation.",
    ],
    practice: [
      "Compare a word’s embedding across several senses and layers.",
      "Fine-tune a small encoder for document classification.",
      "Implement subword-to-word label alignment for NER.",
      "Evaluate frozen features versus full fine-tuning.",
      "Run multiple seeds and report variance on a small dataset.",
    ],
    faq: [
      ["Why are masked models good classifiers?", "They encode the entire input bidirectionally and can provide a compact contextual representation to a task head."],
      ["Can BERT generate text?", "It can fill masks and be adapted to generation-like procedures, but its pretraining objective is not efficient left-to-right generation."],
      ["What is fine-tuning?", "Continue training the pretrained model on labeled task data, usually with a new output head and a smaller learning rate."],
    ],
    takeaways: [
      "Masked encoders use both left and right context.",
      "Self-supervised corruption turns raw text into training examples.",
      "Contextual vectors represent a token’s current use.",
      "Fine-tuning adapts general features to specific labels.",
      "Tokenization alignment is a first-class sequence-labeling concern.",
    ],
  },
  {
    n: 11,
    slug: "information-retrieval-and-rag",
    title: "Information Retrieval and Retrieval-Augmented Generation",
    focus: "Sparse and dense retrieval, inverted indexes, ranking, evaluation, question answering, retrieval-augmented generation, grounding, and datasets.",
    oneMinute: "Information retrieval selects useful documents or passages from a collection. Sparse retrieval matches weighted terms through an inverted index; dense retrieval compares learned query and passage vectors. Ranking is evaluated with relevance judgments using measures such as precision, recall, mean reciprocal rank, and discounted cumulative gain. Retrieval-augmented generation adds selected evidence to a language model’s context. RAG can improve freshness and attribution, but it creates a chain of possible failures: indexing, query formulation, retrieval, reranking, context construction, generation, and citation. The answer is only as grounded as the evidence actually used.",
    concepts: [
      ["Sparse retrieval is lexical evidence at scale", "An inverted index maps terms to documents. TF–IDF and BM25 reward terms that are important in a document but uncommon across the collection. Sparse systems are fast, interpretable, and strong when query and document share vocabulary."],
      ["Dense retrieval learns semantic matching", "A dual encoder maps queries and passages into a shared vector space and retrieves nearest neighbors. It can connect paraphrases without exact term overlap, but training negatives, domain shift, vector-index settings, and embedding updates strongly affect quality."],
      ["Ranking metrics depend on position", "Precision and recall summarize retrieved relevance; reciprocal rank emphasizes the first relevant result; nDCG gives graded relevance more credit near the top. Offline judgments must reflect real information needs."],
      ["RAG is a multi-stage system", "Documents are chunked and indexed; a user request becomes a query; candidates are retrieved and perhaps reranked; passages are packed into a prompt; a generator answers; citations are mapped back. Debugging requires measuring each stage separately."],
      ["Grounding must be verified", "Providing a passage does not guarantee the model follows it. Answers can mix prior model knowledge with retrieved text, cite a related but non-supporting source, or ignore contradictory evidence. Entailment or claim-level citation checks provide stronger evidence than a decorative source list."],
    ],
    worked: {
      title: "Answering an internal policy question",
      text: "Chunk the current travel policy by meaningful sections and preserve document version metadata. Retrieve with BM25 and a dense model, union the candidates, and rerank them. Give the generator only the top supporting passages and require sentence-level citations. If the answer says international flights require approval, a verifier should locate the exact supporting policy sentence and reject the claim if the retrieved version is expired. Measure retrieval recall before blaming the generator.",
    },
    pitfalls: [
      "Using arbitrary fixed-size chunks that split definitions from their conditions.",
      "Evaluating answer style while never measuring whether the required passage was retrieved.",
      "Treating any citation to the right document as support for the specific claim.",
      "Failing to remove stale, duplicate, or access-restricted documents.",
      "Updating the embedding model without rebuilding or versioning the index.",
    ],
    practice: [
      "Build a BM25 baseline and a dense-retrieval baseline.",
      "Create query-level relevance judgments and measure recall@k and MRR.",
      "Test several chunking strategies on cross-section questions.",
      "Log retrieved passages alongside every generated answer.",
      "Score citation support at the claim level.",
    ],
    faq: [
      ["Does RAG eliminate hallucination?", "No. It supplies evidence and can improve grounding, but retrieval or generation can still fail. Verification and abstention remain necessary."],
      ["Sparse or dense retrieval?", "Use both as baselines. Sparse excels at exact identifiers and rare terms; dense helps with paraphrase and conceptual match. Hybrid retrieval often works well."],
      ["What should I measure first?", "Whether a relevant passage appears in the candidate set. A generator cannot use evidence it never receives."],
    ],
    takeaways: [
      "Retrieval is a ranked decision process, not a database lookup.",
      "Sparse and dense methods provide complementary evidence.",
      "RAG adds several measurable failure boundaries.",
      "Chunking, metadata, and index versioning shape correctness.",
      "Citations must support claims, not merely accompany them.",
    ],
  },
  {
    n: 12,
    slug: "machine-translation",
    title: "Machine Translation",
    focus: "Cross-language divergence, encoder-decoder translation, attention, beam search, low-resource translation, evaluation, and bias.",
    oneMinute: "Machine translation maps a source-language sequence into a target-language sequence while preserving meaning, intent, and appropriate form. Languages differ in word order, morphology, pronouns, politeness, and what information must be expressed. Encoder-decoder transformers model translation conditionally; beam search keeps several partial hypotheses during decoding. Low-resource settings use multilingual transfer, back-translation, synthetic data, or related languages. BLEU, chrF, learned metrics, and human evaluation each capture different aspects. A fluent output can still omit content, hallucinate facts, choose the wrong register, or amplify gender and cultural bias.",
    concepts: [
      ["Translation is not word substitution", "A source phrase may require reordering, splitting, combining, or making implicit information explicit. Typological differences mean a correct target can have a very different surface structure from the source."],
      ["Encoder-decoder conditional generation", "The encoder represents the source; the decoder predicts target tokens while attending to source states and previous target tokens. Cross-attention provides a soft alignment between what is being generated and source evidence."],
      ["Beam search explores partial translations", "Instead of committing to one next token, keep the highest-scoring partial sequences. Length normalization and stopping rules matter because raw sequence probabilities favor shorter outputs. A wider beam is not always better."],
      ["Low-resource translation borrows signal", "Multilingual models share parameters across languages. Back-translation turns target-only text into synthetic parallel data. Transfer works best when scripts, domains, or linguistic structures overlap, but synthetic errors can reinforce themselves."],
      ["Evaluation needs adequacy and fluency", "BLEU measures n-gram overlap, chrF uses character sequences, learned metrics compare contextual representations, and humans judge meaning, grammar, terminology, and style. No automatic metric fully replaces bilingual review for consequential use."],
    ],
    worked: {
      title: "Translating a polite request",
      text: "The English “Could you send the report?” is grammatically a question but pragmatically a request. A target language may require an honorific or a different verb form based on social relationship. The model must preserve the speech act, not translate each word literally. Beam candidates may differ in formality; a glossary and metadata about audience can guide selection. Evaluation should flag a fluent but overly casual translation as a real error.",
    },
    pitfalls: [
      "Evaluating against one reference as if only one translation is valid.",
      "Using a wider beam without checking length bias and diversity.",
      "Letting names, numbers, negation, or units change silently.",
      "Assuming high-resource performance transfers to dialects and low-resource languages.",
      "Ignoring register, gender, and cultural implications.",
    ],
    practice: [
      "Compare literal and meaning-preserving translations for five divergent constructions.",
      "Inspect cross-attention or alignments, while treating them as diagnostic rather than proof.",
      "Run greedy and beam decoding with length normalization.",
      "Evaluate with BLEU or chrF plus a bilingual error taxonomy.",
      "Create a terminology and named-entity preservation test set.",
    ],
    faq: [
      ["Why can a valid translation score poorly on BLEU?", "It may use different but correct wording from the limited references. Overlap metrics undercount legitimate variation."],
      ["What is back-translation?", "Translate monolingual target text into the source language to create synthetic parallel pairs, then train on them."],
      ["Why does beam width sometimes hurt?", "The model’s probability and length biases can make a larger search find bland or too-short sequences that score well internally."],
    ],
    takeaways: [
      "Translation preserves communicative meaning across different linguistic systems.",
      "Cross-attention connects target generation to source evidence.",
      "Decoding and length handling affect output quality.",
      "Low-resource methods transfer and synthesize supervision.",
      "Human adequacy, terminology, and social meaning remain essential checks.",
    ],
  },
  {
    n: 13,
    slug: "rnns-and-lstms",
    title: "RNNs and LSTMs",
    focus: "Recurrent neural networks, sequence modeling, stacked and bidirectional architectures, long short-term memory, encoder-decoder models, and attention.",
    oneMinute: "A recurrent neural network processes a sequence one step at a time, updating a hidden state that summarizes the past. It naturally handles variable-length input and can serve as a language model or sequence classifier. Plain RNNs struggle to learn long-range dependencies because gradients vanish or explode. LSTMs introduce gated memory paths that control writing, keeping, and reading information. Bidirectional RNNs use both directions for complete-input tasks; encoder-decoder RNNs map one sequence to another; attention lets the decoder consult all encoder states instead of compressing everything into one vector. Transformers now dominate many tasks, but RNNs remain the clearest path to understanding sequence state and gating.",
    concepts: [
      ["Recurrence shares one transition", "At each step, the network combines the current input with the previous hidden state. The same parameters are reused across time, giving a model that can process sequences of different lengths."],
      ["Backpropagation unfolds through time", "Training treats the recurrent computation as a deep network with one copy per time step. Gradients multiply repeatedly through the transition, which can shrink toward zero or grow unstably."],
      ["LSTM gates protect memory", "An LSTM uses input, forget, and output gates around a cell state. The additive memory path makes it easier for gradients and information to persist. Gates learn when to overwrite or expose state."],
      ["Direction depends on the task", "A causal language model can only use past tokens. A bidirectional tagger can use both past and future because the complete sentence is available. Using future context in an online task would be leakage."],
      ["Attention removes the single-vector bottleneck", "An encoder-decoder without attention forces the entire source into one final state. Attention computes a weighted combination of encoder states for each output step, creating a flexible alignment and foreshadowing transformers."],
    ],
    worked: {
      title: "Remembering negation across a review",
      text: "In “I expected to love it, but I did not enjoy the ending,” a recurrent sentiment model must carry early expectations, notice the contrast, and let “not” influence “enjoy.” A plain RNN may lose long-range signals. An LSTM can retain relevant state while forgetting details. A bidirectional classifier can also use later words when representing earlier positions, but a live next-word predictor cannot.",
    },
    pitfalls: [
      "Using bidirectional context in a streaming or causal evaluation.",
      "Ignoring padding masks when batching different sequence lengths.",
      "Assuming LSTMs completely solve long-range dependency problems.",
      "Failing to clip exploding gradients.",
      "Comparing an unoptimized RNN baseline with a heavily tuned transformer.",
    ],
    practice: [
      "Unroll a tiny RNN for three time steps and track shapes.",
      "Plot gradient norms as sequence length grows.",
      "Compare plain RNN and LSTM on a delayed-memory task.",
      "Implement masking for padded batches.",
      "Add attention to an encoder-decoder and inspect alignments.",
    ],
    faq: [
      ["Why do gradients vanish?", "Repeated multiplication by transition derivatives smaller than one shrinks the signal as it travels back through many steps."],
      ["What does the forget gate do?", "It chooses how much of the previous cell state to retain, allowing the model to clear obsolete information."],
      ["Are RNNs obsolete?", "No. They remain useful for some streaming, small, or low-latency settings and are foundational for understanding sequential state."],
    ],
    takeaways: [
      "RNNs summarize a sequence in a recurrent hidden state.",
      "Backpropagation through time creates long-gradient paths.",
      "LSTM gates create a more durable memory route.",
      "Bidirectionality is powerful only when future input is legitimately available.",
      "RNN attention is the conceptual bridge to transformers.",
    ],
  },
  {
    n: 14,
    slug: "phonetics-and-speech-features",
    title: "Phonetics and Speech Feature Extraction",
    focus: "Speech sounds, phonetic transcription, articulation, prosody, acoustic signals, spectrograms, log-Mel features, and MFCCs.",
    oneMinute: "Speech begins as a time-varying pressure signal, not a sequence of letters. Phonetics describes how sounds are produced and perceived; transcription systems such as the IPA and ARPAbet name sound categories. Articulatory features describe the vocal tract, while acoustic analysis examines frequency, amplitude, harmonics, formants, and timing. Because raw waveforms are large and phase-sensitive, classic speech systems divide audio into short overlapping frames, compute spectra, map frequencies to the perceptually motivated Mel scale, and take log energies. MFCCs further decorrelate these features. Prosody—pitch, duration, energy, and rhythm—carries emphasis, emotion, phrasing, and conversational meaning.",
    concepts: [
      ["Phones are physical; phonemes are contrastive", "A phone is a concrete speech sound. A phoneme is a language-specific category whose distinctions can change meaning. The same acoustic variation may be meaningful in one language and incidental in another."],
      ["Articulation explains sound families", "Consonants vary by place and manner of constriction and voicing; vowels vary by tongue height, backness, and lip rounding. These dimensions help explain systematic confusions and pronunciation variation."],
      ["Speech is nonstationary but locally stable", "The waveform changes rapidly, yet a 20–30 ms frame is often treated as approximately stationary. Windowing and overlap reduce boundary artifacts before a Fourier transform reveals frequency energy."],
      ["Mel features approximate auditory resolution", "A filterbank groups spectral energy with finer resolution at lower frequencies and coarser resolution at higher frequencies. Taking logarithms reflects perceptual loudness and turns multiplicative effects into more manageable additive differences."],
      ["Prosody lives above segments", "Fundamental frequency, energy, duration, pauses, and rhythm express sentence type, focus, emotion, turn-taking, and speaker style. A transcript discards much of this information."],
    ],
    worked: {
      title: "From waveform to log-Mel spectrogram",
      text: "Resample audio to a fixed rate, remove or record channel assumptions, split it into overlapping frames, apply a window, and compute a power spectrum. Multiply by Mel-spaced triangular filters and take the logarithm. The resulting time-by-frequency image makes vowels, fricatives, silence, and transitions easier for a model to distinguish. Normalization must be defined consistently; otherwise microphone or speaker statistics can leak across train and test.",
    },
    pitfalls: [
      "Treating written spelling as a reliable pronunciation.",
      "Mixing sample rates, channels, or amplitude conventions silently.",
      "Computing features over padded audio without a mask.",
      "Removing silence or disfluency that the task needs.",
      "Evaluating only on studio speech and deploying on noisy, accented, far-field audio.",
    ],
    practice: [
      "Record vowels and inspect waveforms and spectrograms.",
      "Label voicing, place, and manner for a small phone set.",
      "Implement framing, windowing, FFT, and Mel filtering.",
      "Compare log-Mel features and MFCCs.",
      "Measure pitch and duration changes in a question versus a statement.",
    ],
    faq: [
      ["Why are frames overlapped?", "Overlap preserves events near window boundaries and gives smoother time resolution."],
      ["What is a formant?", "A resonance of the vocal tract visible as a concentration of spectral energy; the first formants are especially informative for vowel identity."],
      ["Do modern models still need hand-designed features?", "Some learn directly from waveforms, but log-Mel features remain efficient, robust, and widely used. The signal-processing concepts still explain model behavior."],
    ],
    takeaways: [
      "Speech is a physical signal shaped by articulation and perception.",
      "Short-time spectral analysis converts waveforms into usable structure.",
      "Mel and log transformations encode perceptual assumptions.",
      "Prosody carries information beyond the words.",
      "Audio conditions and normalization are part of the dataset definition.",
    ],
  },
  {
    n: 15,
    slug: "automatic-speech-recognition",
    title: "Automatic Speech Recognition",
    focus: "The ASR task, convolutional encoders, encoder-decoder recognition, self-supervised speech models, CTC, decoding, and word error rate.",
    oneMinute: "Automatic speech recognition converts an acoustic sequence into text. The central difficulty is alignment: thousands of audio frames correspond to far fewer output tokens, and boundaries are not given. Convolutional or transformer encoders turn local acoustic features into contextual states. Attention-based encoder-decoder systems learn a flexible mapping; Connectionist Temporal Classification sums over monotonic frame-to-label alignments using a blank symbol. Self-supervised models such as HuBERT learn speech representations from unlabeled audio before supervised adaptation. Word error rate counts substitutions, deletions, and insertions, but it hides who is affected and how costly each error is.",
    concepts: [
      ["ASR maps long acoustics to short symbols", "Audio may provide 50–100 frames per second while text has only a few tokens. Speaking rate, coarticulation, silence, and noise make frame boundaries uncertain. Alignment is therefore a modeling problem, not preprocessing trivia."],
      ["Encoders build contextual acoustic representations", "Convolutions capture local time-frequency patterns and can downsample the sequence. Transformer or recurrent layers add longer context. The encoder must preserve content while becoming robust to speaker, channel, and background variation."],
      ["CTC marginalizes monotonic alignments", "CTC introduces a blank symbol and allows repeated labels, then collapses paths to the same output. Dynamic programming sums path probabilities without requiring frame-level labels. Its conditional-independence assumptions simplify training but limit direct language modeling."],
      ["Self-supervision uses unlabeled audio", "Models predict masked or clustered speech units from context, learning phonetic and speaker-relevant structure before seeing transcripts. This is especially valuable because audio is plentiful and accurate transcription is expensive."],
      ["WER is edit distance over words", "WER equals substitutions plus deletions plus insertions divided by reference word count. It is easy to compute but weights all word errors equally and depends on text normalization. Entity error, semantic error, and subgroup performance may matter more."],
    ],
    worked: {
      title: "Recognizing “deploy at four”",
      text: "The encoder turns noisy frames into contextual vectors. CTC may assign blank to most frames, repeat labels across several frames, and collapse them into a token sequence. A language-model or beam-search decoder helps prefer “deploy at four” over acoustically similar alternatives. Yet if the application executes commands, confusing “four” with “for” may be harmless while confusing “deploy” with “destroy” is critical. Evaluation must reflect semantic cost, not only aggregate WER.",
    },
    pitfalls: [
      "Comparing WERs with different punctuation, casing, or number-normalization rules.",
      "Reporting overall WER while hiding accent, dialect, age, gender, or noise disparities.",
      "Leaking speakers or near-duplicate recordings across splits.",
      "Using a language model so strong that it replaces unusual names with common phrases.",
      "Ignoring streaming latency and endpointing in a real-time product.",
    ],
    practice: [
      "Compute WER and backtrace the word alignment.",
      "Train or inspect a CTC model on a tiny vocabulary.",
      "Compare greedy CTC decoding with beam search.",
      "Create entity and command-critical error metrics.",
      "Stratify errors by speaker and acoustic condition.",
    ],
    faq: [
      ["What does the CTC blank mean?", "It represents frames that emit no new output symbol and separates repeated labels before collapse."],
      ["Why can a lower WER system be worse?", "It may make fewer total errors but more errors on names, numbers, commands, or user groups that matter."],
      ["What is endpointing?", "Detecting when an utterance has begun or ended so a streaming system can respond without clipping speech or waiting too long."],
    ],
    takeaways: [
      "Alignment is the core structural problem in ASR.",
      "Encoders remove acoustic variation while preserving linguistic content.",
      "CTC trains without frame-level transcripts.",
      "Self-supervision reduces dependence on labeled audio.",
      "WER needs normalization rules and task-specific companion metrics.",
    ],
  },
  {
    n: 16,
    slug: "text-to-speech",
    title: "Text-to-Speech",
    focus: "TTS pipelines, audio codecs and discrete tokens, language-model speech generation, VALL-E-style two-stage systems, evaluation, other speech tasks, and spoken language models.",
    oneMinute: "Text-to-speech produces intelligible, natural, and appropriately styled audio from text. Modern systems may convert waveforms into compact discrete codec tokens, predict those tokens with a language-model architecture conditioned on text and speaker cues, and decode them back to audio. Two-stage designs often generate coarse semantic or acoustic structure first and fine detail second. Quality is multidimensional: intelligibility, naturalness, speaker similarity, prosody, latency, robustness, and safety all matter. Voice cloning introduces consent, impersonation, watermarking, and access-control requirements that cannot be solved by audio quality alone.",
    concepts: [
      ["TTS separates content from acoustic realization", "The text specifies words but not complete pronunciation, timing, emphasis, emotion, or voice. The model must infer or receive these controls. Text normalization expands numbers and abbreviations before acoustic generation."],
      ["Neural codecs create discrete audio tokens", "An encoder compresses waveform segments into codebook indices and a decoder reconstructs audio. Multiple residual codebooks can represent coarse-to-fine detail. Compression makes audio compatible with token-prediction models but introduces reconstruction limits."],
      ["Language models can generate speech tokens", "Conditioned on text and possibly a short speaker prompt, a model predicts codec-token sequences. A second stage may fill finer codebooks. This reframes speech synthesis as conditional sequence modeling while retaining a waveform decoder."],
      ["Prosody and speaker identity are distinct controls", "A system may preserve voice timbre while changing speaking rate or emotion, or imitate prosody without matching identity. Evaluation should separate these dimensions rather than using one naturalness score."],
      ["Responsible synthesis is a system property", "Consent records, speaker enrollment, rate limits, watermarking or provenance, abuse monitoring, and restricted high-risk use cases surround the model. A realistic-sounding demo is not a complete product."],
    ],
    worked: {
      title: "Synthesizing an emergency announcement",
      text: "Normalize “Gate B12 closes at 18:05” into an unambiguous spoken form. Condition the model on a clear approved voice and a calm, urgent style. Generate coarse tokens, then acoustic detail, decode the waveform, and check pronunciation of the gate and time. Intelligibility and latency matter more than dramatic expressiveness. The pipeline should refuse an unapproved cloned voice and attach provenance metadata.",
    },
    pitfalls: [
      "Evaluating naturalness while ignoring intelligibility and semantic mistakes.",
      "Using a short voice sample without consent or identity controls.",
      "Failing on numbers, abbreviations, names, and code-switching during text normalization.",
      "Measuring quality only on short, clean sentences.",
      "Treating watermarking as a substitute for access control and abuse response.",
    ],
    practice: [
      "Write normalization rules for dates, money, abbreviations, and identifiers.",
      "Compare waveform, spectrogram, and codec-token representations.",
      "Evaluate intelligibility with ASR plus human listening.",
      "Separate naturalness, speaker similarity, and prosody in a listening test.",
      "Draft a consent and misuse-threat checklist for voice cloning.",
    ],
    faq: [
      ["What is a neural audio codec?", "A learned compressor that maps waveform segments to compact latent or discrete codes and reconstructs audio from them."],
      ["Why use two stages?", "Coarse tokens capture broad content and timing; later stages add fine acoustic detail, reducing the burden on one sequence model."],
      ["Can ASR score TTS?", "ASR can estimate intelligibility, but it cannot fully judge naturalness, speaker identity, emotion, or listening comfort."],
    ],
    takeaways: [
      "TTS must infer acoustic form that text leaves unspecified.",
      "Codec tokens make waveform generation a sequence-modeling problem.",
      "Coarse-to-fine generation separates structure from detail.",
      "Quality has several independent dimensions.",
      "Voice synthesis needs consent, provenance, and misuse controls.",
    ],
  },
  {
    n: 17,
    slug: "sequence-labeling-pos-ner",
    title: "Sequence Labeling for Parts of Speech and Named Entities",
    focus: "Word classes, part-of-speech tagging, named entities, HMMs, conditional random fields, neural sequence labeling, and span-level evaluation.",
    oneMinute: "Sequence labeling assigns a category to each token or span. Part-of-speech tagging identifies grammatical roles; named entity recognition finds people, organizations, locations, dates, and domain-specific types. Labels are not independent: valid sequences follow patterns, and context disambiguates words. Hidden Markov models combine transition and emission probabilities; conditional random fields score a whole label sequence using overlapping features; neural encoders learn contextual representations. Entity evaluation is usually span-level exact match, so boundary and type must both be correct.",
    concepts: [
      ["Tags describe roles in context", "The same word can be a noun, verb, or adjective depending on its sentence. Tagsets are annotation schemes, not universal natural categories; granularity and guidelines determine what a model can learn."],
      ["Entities are typed spans", "NER must decide both boundaries and labels. BIO-style tags encode beginning, inside, and outside positions, but subword tokenization and nested entities complicate the simple scheme."],
      ["HMMs are generative sequence models", "An HMM models tag transitions and word emissions, then uses dynamic programming such as Viterbi to find the most probable tag sequence. Its independence assumptions are restrictive but transparent."],
      ["CRFs normalize over full label sequences", "A linear-chain CRF combines token features with transition scores and trains discriminatively. It can discourage impossible transitions, such as an inside tag without a matching beginning, while considering the full sequence."],
      ["Evaluation should be span-aware", "Token accuracy can look high because most tokens are outside entities. Exact entity precision, recall, and F1 require the predicted span and type to match. Partial-overlap analysis helps diagnose boundary errors."],
    ],
    worked: {
      title: "Tagging “Apple hired Martin in Pune”",
      text: "Context tells us “Apple” is an organization rather than fruit, “Martin” is a person, and “Pune” is a location. A BIO sequence might mark B-ORG, O, B-PER, O, B-LOC. An HMM uses word and tag patterns; a contextual encoder uses the whole sentence; a CRF layer enforces coherent transitions. If “Martin in Pune” is predicted as one person span, token accuracy may remain high while entity-level scoring correctly counts a boundary failure.",
    },
    pitfalls: [
      "Reporting token accuracy for sparse entity labels.",
      "Leaking document templates or repeated entities across splits.",
      "Ignoring nested, discontinuous, or domain-specific entities.",
      "Mishandling BIO labels after subword tokenization.",
      "Assuming annotation guidelines are consistent across datasets.",
    ],
    practice: [
      "Tag ambiguous words and explain which context resolves them.",
      "Implement Viterbi decoding for a tiny HMM.",
      "Validate BIO transition legality.",
      "Compute exact span precision, recall, and F1.",
      "Build an error table separating boundary and type mistakes.",
    ],
    faq: [
      ["Why not label each token independently?", "Neighboring labels constrain one another and entities span multiple tokens. Sequence models use these dependencies."],
      ["What does BIO mean?", "B begins an entity, I continues it, and O is outside. Type suffixes such as B-PER identify the entity class."],
      ["Are named entities fixed categories?", "No. Product needs determine the schema: medicine, finance, legal text, and general news require different types and guidelines."],
    ],
    takeaways: [
      "Labels are contextual and scheme-dependent.",
      "NER predicts boundaries and types together.",
      "HMMs, CRFs, and neural encoders express increasing feature flexibility.",
      "Structured decoding prevents incoherent label sequences.",
      "Span-level evaluation reveals errors token accuracy hides.",
    ],
  },
  {
    n: 18,
    slug: "context-free-grammars-constituency-parsing",
    title: "Context-Free Grammars and Constituency Parsing",
    focus: "Constituents, context-free grammars, treebanks, ambiguity, normal forms, CKY dynamic programming, neural span parsing, and evaluation.",
    oneMinute: "Constituency parsing groups words into nested phrases such as noun phrases and verb phrases. A context-free grammar defines how categories expand into sequences of categories or words. The same sentence can support several parse trees, so parsing searches a large structured space. Treebanks provide annotated examples. CKY parsing uses dynamic programming over spans, usually after converting a grammar to Chomsky Normal Form. Modern neural parsers score spans and labels directly, but the core questions remain: which word groups form units, how are they nested, and how should ambiguous analyses be evaluated?",
    concepts: [
      ["Constituents are reusable phrase units", "Substitution, movement, and coordination tests suggest that word groups behave as units, but tests can disagree. A parse tree is an analysis under a grammar and annotation convention, not a photograph of an uncontested structure."],
      ["CFG rules build recursive trees", "Rules such as S → NP VP and NP → Det N allow phrases to contain phrases. Context-free means the left-hand category expands without directly consulting its surrounding context, which provides tractability but misses some constraints."],
      ["Ambiguity multiplies analyses", "Prepositional-phrase attachment, coordination, and noun compounds can produce many valid trees. A parser needs probabilities or learned scores to rank them using lexical and contextual evidence."],
      ["CKY reuses span solutions", "For every span and category, CKY considers split points and combines best subparses. Dynamic programming avoids recomputing the same subspans and guarantees an optimal parse under the model assumptions."],
      ["Neural span parsers score constituents", "A contextual encoder represents boundaries; a model scores whether each span has a label and chooses a compatible tree. This reduces hand-designed grammar dependence while still producing explicit nested structure."],
    ],
    worked: {
      title: "Attaching “with the telescope”",
      text: "In “I saw the scientist with the telescope,” the phrase may modify “saw” (I used the telescope) or “scientist” (the scientist had it). Both trees satisfy a basic grammar. CKY constructs both from shared subspans. A statistical or neural score uses lexical tendencies and broader context to rank them. The parser should preserve uncertainty when evidence is weak rather than presenting syntax as certain meaning.",
    },
    pitfalls: [
      "Treating one treebank convention as the only correct syntax.",
      "Converting to normal form and forgetting to map output back to original labels.",
      "Ignoring unknown words or grammar coverage failures.",
      "Using sentence-level exact match as the only diagnostic.",
      "Assuming a high parse F1 guarantees correct downstream meaning.",
    ],
    practice: [
      "Draw two trees for an attachment ambiguity.",
      "Convert a small CFG into Chomsky Normal Form.",
      "Fill a CKY chart by hand for a short sentence.",
      "Compute labeled span precision, recall, and F1.",
      "Compare parser errors by phrase type and sentence length.",
    ],
    faq: [
      ["What is a constituent?", "A sequence of words treated as a unit in a particular syntactic analysis, commonly represented as a labeled tree span."],
      ["Why does CKY need normal form?", "Binary rules let each span be formed by combining exactly two smaller spans at a split point, simplifying the recurrence."],
      ["Do transformers already know syntax?", "They encode useful syntactic patterns, but an explicit parser is still valuable when a task needs inspectable, constrained tree structure."],
    ],
    takeaways: [
      "Constituency represents nested phrase structure.",
      "CFGs generate recursive candidate trees.",
      "Ambiguity requires scoring, not only rule matching.",
      "CKY makes exhaustive structured search tractable.",
      "Parse metrics evaluate spans under a chosen annotation scheme.",
    ],
  },
  {
    n: 19,
    slug: "dependency-parsing",
    title: "Dependency Parsing",
    focus: "Head-dependent relations, dependency trees, transition-based parsing, graph-based parsing, projectivity, decoding, and attachment evaluation.",
    oneMinute: "Dependency syntax connects each word to a head with a labeled relation such as subject, object, or modifier. The result is a directed tree rooted at a special root. Transition-based parsers build the tree through actions such as shift and arc creation; graph-based parsers score possible edges and search for the best tree. Projective trees have no crossing arcs in the word order, while many languages require non-projective analyses. Evaluation uses unlabeled and labeled attachment scores, but punctuation policy, tokenization, and annotation scheme must match.",
    concepts: [
      ["Dependencies center lexical heads", "Instead of grouping words into phrase boxes, a dependency tree says which word governs which. Relations expose predicate–argument and modifier structure compactly, often making them useful for extraction and multilingual analysis."],
      ["Transition systems parse incrementally", "A stack, buffer, and set of arcs define a parser state. A classifier selects legal actions. Greedy parsing is fast but early mistakes can cascade; beam search keeps alternatives at additional cost."],
      ["Graph-based parsing scores edges", "Treat words as nodes and candidate dependencies as weighted edges. First-order models score edges independently, then a maximum-spanning-tree or projective dynamic program chooses a legal global tree."],
      ["Projectivity is an ordering constraint", "An arc is projective when its subtree occupies a contiguous region and arcs do not cross when drawn above the sentence. Free word order and long-distance constructions can require non-projective parsing."],
      ["Attachment metrics have conventions", "UAS checks whether each token chose the correct head; LAS also requires the relation label. Whether punctuation counts and how multiword tokens are represented can change the number substantially."],
    ],
    worked: {
      title: "Parsing a service alert",
      text: "In “The database rejected the connection,” “rejected” is the root predicate, “database” attaches as subject, and “connection” as object. A transition parser might shift tokens and create arcs incrementally. A graph parser scores every plausible head and chooses the highest-scoring legal tree. If it attaches “database” to “connection,” only one head error occurs numerically, but downstream extraction may reverse who did what.",
    },
    pitfalls: [
      "Comparing attachment scores across different tokenization or punctuation conventions.",
      "Using greedy transitions without analyzing error propagation.",
      "Forcing projectivity on languages or constructions that need crossing dependencies.",
      "Treating dependency labels as universal across treebanks.",
      "Assuming a tree captures semantic scope, coreference, or discourse automatically.",
    ],
    practice: [
      "Convert five simple sentences into head–dependent arcs.",
      "Simulate an arc-standard or arc-eager transition sequence.",
      "Find a maximum spanning tree in a tiny scored graph.",
      "Compute UAS and LAS with an explicit punctuation rule.",
      "Group errors by dependency relation and distance.",
    ],
    faq: [
      ["Dependency or constituency parsing?", "They emphasize different structure. Dependencies connect lexical heads directly; constituency groups nested phrases. Conversion is possible but not lossless across schemes."],
      ["What is error propagation?", "A wrong early transition changes the parser state and can force later mistakes even if the action classifier would otherwise be correct."],
      ["Why require a tree?", "The constraint ensures one connected, acyclic head structure. Some semantic representations use graphs instead because meaning can require reentrancy."],
    ],
    takeaways: [
      "Dependency trees encode who depends on which head.",
      "Transition and graph parsers trade local speed against global search.",
      "Structural constraints are part of decoding.",
      "Projectivity is language- and construction-sensitive.",
      "Attachment scores need transparent conventions and error analysis.",
    ],
  },
  {
    n: 20,
    slug: "information-extraction-relations-events-time",
    title: "Information Extraction: Relations, Events, and Time",
    focus: "Relation extraction, event extraction, temporal representation, aspect, TimeBank-style annotation, temporal analysis, and template filling.",
    oneMinute: "Information extraction converts unstructured text into structured facts: relations between entities, events with participants, temporal expressions and links, or filled templates for a domain. A relation classifier must distinguish the target pair and direction. Event systems identify triggers and arguments. Temporal analysis normalizes expressions, represents event time and aspect, and orders events relative to one another and the document time. The difficult part is not only detection; it is defining a schema, handling negation and uncertainty, linking mentions across sentences, and avoiding unsupported database-like certainty.",
    concepts: [
      ["Relations are typed, directed links", "Examples include employed-by, located-in, treats, or acquired. The entity types, mention context, syntax, and document evidence help classification. “A acquired B” is not the same direction as “B acquired A.”"],
      ["Events have triggers and arguments", "A trigger evokes an event type; arguments fill roles such as agent, patient, instrument, location, or time. The trigger may be a verb, noun, or multiword expression, and some events are implied rather than explicit."],
      ["Time needs normalization and anchoring", "“Next Friday” depends on the document creation time; “two days later” depends on another event. Systems normalize expressions to calendar values or intervals and label temporal relations such as before, after, or overlap."],
      ["Aspect describes event shape", "Whether an event is ongoing, completed, habitual, or instantaneous affects temporal reasoning. Tense alone does not determine real-world time, especially in reported, hypothetical, or planned statements."],
      ["Template filling is document-level assembly", "A template may require one incident’s participants, location, cause, and outcome from several sentences. Coreference, relation consistency, missing values, and confidence management matter as much as local extraction."],
    ],
    worked: {
      title: "Extracting an outage timeline",
      text: "From “Payments failed at 09:02. Engineers rolled back the release ten minutes later, restoring service by 09:19,” extract a failure event, rollback event, and restoration event. Link engineers as rollback agents, the release as the rolled-back object, and service as restoration target. Normalize “ten minutes later” relative to 09:02 and “by 09:19” as an upper bound. Do not invent the outage cause unless another sentence supports it.",
    },
    pitfalls: [
      "Collapsing negated, hypothetical, planned, and completed events.",
      "Ignoring relation direction or argument roles.",
      "Normalizing relative dates without document time and timezone.",
      "Treating one mention as a unique real-world event without coreference.",
      "Writing low-confidence extractions into a knowledge base as certain facts.",
    ],
    practice: [
      "Define a small event schema before training a model.",
      "Annotate triggers, roles, negation, and modality in ten sentences.",
      "Normalize relative times against a fixed document date.",
      "Build a document-level template with provenance for every field.",
      "Measure detection, classification, and argument-linking errors separately.",
    ],
    faq: [
      ["How is relation extraction different from NER?", "NER finds typed spans; relation extraction connects selected entities with typed links."],
      ["Why is time difficult?", "Expressions are relative, underspecified, timezone-sensitive, and tied to event aspect and discourse rather than only verb tense."],
      ["What should a stored fact include?", "The extracted value, source span, document identity and version, timestamp, model version, and confidence or review state."],
    ],
    takeaways: [
      "Information extraction begins with a task-specific schema.",
      "Relations and events require direction and roles.",
      "Temporal meaning needs normalization and anchoring.",
      "Negation, modality, and aspect prevent false factual claims.",
      "Provenance and uncertainty belong in the structured output.",
    ],
  },
  {
    n: 21,
    slug: "semantic-role-labeling",
    title: "Semantic Role Labeling and Argument Structure",
    focus: "Semantic roles, alternations, thematic-role limits, PropBank, FrameNet, SRL systems, selectional restrictions, and predicate decomposition.",
    oneMinute: "Semantic role labeling asks who did what to whom, where, when, and how. Predicates evoke events or states; arguments fill roles. Broad thematic labels are intuitive but inconsistent across predicates, so resources choose different designs. PropBank gives verb-specific numbered roles tied to syntax, while FrameNet organizes words around conceptual frames with named frame elements. SRL systems detect predicates, identify argument spans, and label them. Selectional preferences provide soft expectations, not hard rules. Role structure helps extraction and question answering but does not by itself resolve coreference, negation, time, or factuality.",
    concepts: [
      ["Roles generalize across syntax", "Active and passive sentences can describe the same participants despite different word order. Semantic roles aim to preserve event participation across these alternations."],
      ["Thematic roles are useful but fuzzy", "Labels such as Agent, Patient, Experiencer, Instrument, and Goal capture intuitions, yet many arguments fit several labels or differ subtly. A small universal inventory can oversimplify."],
      ["PropBank is predicate-specific", "A frameset defines numbered arguments such as ARG0 and ARG1 for a particular predicate sense, plus modifiers for time, location, manner, and more. Numbered roles are consistent within a frameset, not universally identical across every verb."],
      ["FrameNet is concept-centered", "Lexical units evoke frames such as Commerce_buy, with frame elements such as Buyer, Goods, Seller, and Money. Different words can evoke the same conceptual situation, supporting richer cross-lexical generalization."],
      ["Selectional restrictions are probabilities", "Predicates prefer certain semantic types—“eat” usually takes edible objects—but metaphor, fiction, and unusual events violate defaults. Modern models treat these as learned preferences rather than rigid filters."],
    ],
    worked: {
      title: "Active and passive deployment events",
      text: "“The engineer deployed the patch” and “The patch was deployed by the engineer” have different syntax but the same deployment roles: engineer as deployer and patch as deployed item. An SRL system should identify the predicate, span both arguments correctly, and assign consistent roles. If the next sentence says “It failed,” coreference and event reasoning are still needed to decide whether “it” means the patch or deployment.",
    },
    pitfalls: [
      "Assuming ARG0 always means Agent and ARG1 always means Patient across all predicates.",
      "Scoring correct labels on incorrect argument boundaries as success.",
      "Confusing syntactic subject/object with semantic role.",
      "Treating selectional preferences as rules that ban novel or figurative language.",
      "Expecting SRL alone to determine truth, time, or reference.",
    ],
    practice: [
      "Label roles in active, passive, and dative alternations.",
      "Compare a PropBank frameset with a related FrameNet frame.",
      "Separate predicate detection, span detection, and role-label errors.",
      "Test metaphors that violate ordinary selectional preferences.",
      "Use SRL output to build a simple who-did-what table with provenance.",
    ],
    faq: [
      ["How is SRL different from dependency parsing?", "Dependencies label syntactic head relations; SRL labels event participants and can map different syntactic forms to similar roles."],
      ["Why use numbered PropBank roles?", "Predicate-specific numbering avoids pretending that one small set of thematic labels cleanly fits every verb."],
      ["What is a frame?", "A structured conceptual situation with participating roles, such as buying with a buyer, seller, goods, and payment."],
    ],
    takeaways: [
      "SRL represents predicate–argument meaning across syntactic variation.",
      "Role inventories are design choices with different strengths.",
      "PropBank and FrameNet organize supervision differently.",
      "Argument boundaries matter as much as labels.",
      "Semantic roles are one layer in a larger meaning pipeline.",
    ],
  },
  {
    n: 22,
    slug: "sentiment-affect-connotation-lexicons",
    title: "Lexicons for Sentiment, Affect, and Connotation",
    focus: "Emotion definitions, sentiment and affect lexicons, human labeling, semi-supervised induction, supervised word sentiment, lexicon-based recognition, entity-centered affect, and connotation frames.",
    oneMinute: "A sentiment or affect lexicon maps words and phrases to properties such as positive/negative valence, emotion categories, intensity, or social connotation. Lexicons can be labeled directly by people, expanded semi-supervised through lexical or embedding relations, or learned from labeled texts. Using a lexicon requires composition: negation, intensifiers, sarcasm, domain meaning, and the target entity can reverse a naive word score. Connotation frames go beyond positive and negative words to model how predicates imply attitudes or effects toward participants. Lexicons are interpretable resources, but their coverage, cultural scope, and annotation assumptions must be explicit.",
    concepts: [
      ["Emotion has multiple representations", "Discrete labels such as joy, anger, fear, and sadness coexist with dimensional models such as valence and arousal. The right representation depends on the application and annotation reliability."],
      ["Human labels need a clear perspective", "Annotators may judge a word’s prior sentiment, its feeling in a sentence, the writer’s attitude, or likely reader response. Mixing these questions creates noisy labels that no model can resolve."],
      ["Lexicons can be expanded through structure", "Seed words propagate labels across semantic graphs, co-occurrence statistics, or embeddings. The method increases coverage but can drift through ambiguous or domain-specific neighbors."],
      ["Composition changes polarity", "“Not good,” “hardly terrible,” and “sick performance” show why word lookup is not sentence understanding. Negation scope, intensification, idioms, and domain conventions must modify or override lexical priors."],
      ["Entity-centered affect asks who is evaluated", "A sentence can praise one participant while harming another. Connotation frames model implied writer perspective, participant effect, value, or agency around a predicate, supporting more precise social analysis."],
    ],
    worked: {
      title: "Why “the patch killed the bug” is not negative overall",
      text: "A naive lexicon sees “killed” and “bug” as negative. In a software context, the sentence expresses a positive outcome for the product and a negative effect on the bug. Entity-centered analysis identifies the patch as effective and the bug as harmed. Domain adaptation and target-aware composition prevent the wrong global sentiment. The example also shows why lexicon scores are features, not final truth.",
    },
    pitfalls: [
      "Summing word polarities without negation, scope, or target.",
      "Using a general lexicon in finance, medicine, slang, or software without validation.",
      "Treating annotator emotion judgments as culturally universal.",
      "Expanding seeds through embeddings without checking semantic drift.",
      "Inferring a person’s private mental state from textual cues.",
    ],
    practice: [
      "Compare discrete emotion and valence–arousal annotations.",
      "Write compositional rules for negation and intensifiers.",
      "Evaluate a lexicon in two domains and inspect polarity shifts.",
      "Annotate which entity receives each attitude or effect.",
      "Audit demographic and cultural coverage of seed words and examples.",
    ],
    faq: [
      ["What is prior polarity?", "The sentiment commonly associated with a word in isolation, before sentence context changes it."],
      ["Why use lexicons with neural models?", "They offer interpretable features, domain resources, weak supervision, and diagnostics even when a neural model handles composition."],
      ["Can text reveal true emotion?", "Text provides cues to expressed or perceived affect, not guaranteed access to a writer’s internal state."],
    ],
    takeaways: [
      "Sentiment and emotion labels depend on the question being asked.",
      "Lexicons can be manual, propagated, or learned.",
      "Context and composition override isolated word polarity.",
      "Affect often targets particular entities rather than a whole sentence.",
      "Cultural and domain validation are required.",
    ],
  },
  {
    n: 23,
    slug: "coreference-and-entity-linking",
    title: "Coreference Resolution and Entity Linking",
    focus: "Mentions, anaphora, coreference clusters, mention detection, ranking architectures, entity linking, evaluation, Winograd-style cases, and gender bias.",
    oneMinute: "Coreference resolution groups mentions that refer to the same discourse entity; entity linking connects a mention to a canonical knowledge-base entry. The pipeline must detect mentions, generate candidates, score compatibility, and form coherent clusters. Signals include agreement, syntax, recency, semantics, speaker, and world knowledge. Neural mention-ranking models compare each mention with candidate antecedents and a no-antecedent option. Entity linking combines mention context with candidate descriptions and popularity. Evaluation is difficult because mention boundaries, clustering, and annotation conventions interact, and gender or occupation stereotypes can create systematic harm.",
    concepts: [
      ["A mention is not the entity", "Names, pronouns, descriptions, and sometimes omitted arguments can refer to the same real or discourse entity. Coreference clusters mentions; it does not automatically identify a real-world database record."],
      ["Mention detection sets the ceiling", "If a mention is missed or its span is wrong, later linking cannot recover it. Some systems detect spans jointly with coreference; others begin from gold mentions in research, which produces an easier and less realistic task."],
      ["Mention ranking compares antecedents", "For each new mention, score earlier spans plus a dummy option for starting a new entity. Pair features and contextual representations capture agreement, distance, syntax, and semantics. Cluster construction must keep decisions globally coherent."],
      ["Entity linking adds a catalog", "Candidate generation finds possible knowledge-base entries from aliases; disambiguation compares the local context and candidate description. NIL handling is essential when the correct entity is absent from the catalog."],
      ["Bias appears in ambiguous links", "Models may use occupational or gender stereotypes to resolve pronouns, producing different decisions for otherwise parallel sentences. Balanced evaluation and counterfactual tests expose these shortcuts."],
    ],
    worked: {
      title: "Linking a person, company, and pronoun",
      text: "“Jordan joined Mercury. They announced a launch.” Mention detection finds Jordan, Mercury, They, and a launch. Context may link “They” to Mercury if the organization announced the event, or to Jordan depending on discourse. Entity linking then decides whether Mercury is a company, planet, or another catalog entity. A good system keeps coreference confidence and knowledge-base confidence separate and supports NIL when no candidate fits.",
    },
    pitfalls: [
      "Evaluating with gold mentions while deployment must detect them.",
      "Forcing every name to a popular knowledge-base entity.",
      "Using agreement rules that exclude singular they or diverse naming conventions.",
      "Scoring only pair decisions rather than cluster quality.",
      "Treating Winograd-style success as broad proof of commonsense reasoning.",
    ],
    practice: [
      "Annotate mentions and clusters in a short article.",
      "Build candidate antecedents with a no-link option.",
      "Separate mention-detection, linking, and clustering errors.",
      "Create NIL examples for entity linking.",
      "Run counterfactual gender and occupation swaps.",
    ],
    faq: [
      ["Coreference versus entity linking?", "Coreference connects mentions within discourse; entity linking maps a mention to an external canonical entity."],
      ["What is an antecedent?", "An earlier mention that a later anaphoric expression refers back to, though some references are not simple backward links."],
      ["Why are coreference metrics complicated?", "They reward different aspects of cluster overlap, so systems usually report several metrics rather than one definitive score."],
    ],
    takeaways: [
      "Mentions, discourse entities, and knowledge-base entities are distinct layers.",
      "Mention detection limits downstream recall.",
      "Ranking balances antecedent evidence with starting a new entity.",
      "NIL is a necessary entity-linking outcome.",
      "Bias testing belongs in ambiguous-reference evaluation.",
    ],
  },
  {
    n: 24,
    slug: "discourse-coherence",
    title: "Discourse Coherence",
    focus: "Coherence relations, discourse structure, centering, entity-based coherence, local representation learning, global coherence, and argument structure.",
    oneMinute: "Discourse coherence explains why a sequence of individually grammatical sentences feels connected and purposeful. Relations such as cause, contrast, elaboration, and evidence connect spans. Discourse parsers predict these relations and a larger structure. Entity-based models track which participants remain salient across sentences; centering theory formalizes local focus transitions. Neural models learn local ordering and coherence signals, while global models examine topic and argument progression across a document. Coherence is not the same as factuality: a fabricated story can be beautifully organized.",
    concepts: [
      ["Relations connect discourse units", "A second sentence may explain, contrast with, result from, or provide evidence for the first. Explicit connectives help, but many relations are implicit and require semantic or world knowledge."],
      ["Structure can be tree-like or graph-like", "Frameworks differ in which spans are elementary, which relations exist, and whether one span is central or supporting. Annotation choice determines the target structure and the meaning of evaluation."],
      ["Centering models local attention", "Each utterance introduces and ranks entities. Smooth discourse tends to maintain or predictably shift a central entity. Pronoun choice and grammatical role provide cues, though genre and language alter the pattern."],
      ["Entity grids expose continuity", "Represent entities by their grammatical roles across sentences. Coherent documents often show stable patterns; shuffled sentences disrupt them. The model is interpretable but misses many semantic relations."],
      ["Global coherence includes argument and topic", "Long documents must introduce a goal, organize supporting points, handle counterarguments, and conclude. Local sentence compatibility cannot guarantee a globally useful report or essay."],
    ],
    worked: {
      title: "Ordering an incident summary",
      text: "A coherent order is: symptom, scope, cause, mitigation, recovery, and follow-up. “Latency rose at 09:02. A bad cache configuration increased database load. Engineers rolled it back. Service recovered at 09:19.” Entities and causal relations progress smoothly. Randomly placing recovery before the cause remains grammatical but weakens the reader’s model. A discourse system can score ordering, yet factual telemetry must still verify every sentence.",
    },
    pitfalls: [
      "Equating fluent transitions with factual or logical validity.",
      "Evaluating only adjacent sentence order while ignoring the document goal.",
      "Applying one discourse annotation scheme as universal.",
      "Ignoring genre—conversation, news, essays, and runbooks organize differently.",
      "Using entity continuity to reward repetitive writing.",
    ],
    practice: [
      "Label cause, contrast, elaboration, and evidence in a short article.",
      "Shuffle paragraphs and explain which coherence cues break.",
      "Build an entity grid for two versions of a document.",
      "Separate local ordering from global argument evaluation.",
      "Check coherence and factual support as independent axes.",
    ],
    faq: [
      ["What is a discourse relation?", "A semantic or pragmatic connection between spans, such as one providing a cause, contrast, condition, or elaboration for another."],
      ["Can an LLM generate coherent text?", "Often yes at the surface and local levels, but long-range goals, evidence, consistency, and truthful argument still require evaluation."],
      ["Why track entities?", "Stable and predictable reference helps readers know what the discourse is about and how new sentences connect."],
    ],
    takeaways: [
      "Coherence arises from relations, focus, topic, and document purpose.",
      "Discourse frameworks define different structures.",
      "Entity continuity is useful but incomplete.",
      "Local fluency does not guarantee global organization.",
      "Coherence and truth must be evaluated separately.",
    ],
  },
  {
    n: 25,
    slug: "conversation-and-structure",
    title: "Conversation and Its Structure",
    focus: "Turn-taking, adjacency pairs, grounding, repair, dialog acts, conversational corpora, context, initiative, and evaluation.",
    oneMinute: "Conversation is coordinated joint action, not alternating isolated sentences. Participants manage turns, signal understanding, repair problems, establish common ground, and perform dialog acts such as questions, requests, confirmations, and commitments. Adjacency pairs create expectations: a question makes an answer relevant; an offer makes acceptance or refusal relevant. Conversational systems must track state across turns, distinguish user intent from literal form, handle interruptions and corrections, and know when to ask or abstain. Evaluation therefore includes task success, recovery, calibration, latency, user effort, and safety—not only response fluency.",
    concepts: [
      ["Turns are actively coordinated", "Speakers use syntax, prosody, gaze, timing, and conventions to project turn completion and manage overlap. Text chat hides some signals but introduces typing indicators, message edits, and asynchronous gaps."],
      ["Dialog acts capture social function", "The sentence form does not uniquely determine the act. “Can you restart it?” is grammatically a question but usually functions as a request. Context and social relationship determine the intended action."],
      ["Adjacency pairs create expectations", "Greeting–greeting, question–answer, request–acceptance/refusal, and repair initiation–repair form organized sequences. Missing or delayed second parts can communicate reluctance or misunderstanding."],
      ["Grounding builds common ground", "Acknowledgments, confirmations, paraphrases, and evidence of successful action show what participants believe is mutually understood. A system should not advance a risky task merely because it produced a fluent response."],
      ["Repair is normal, not exceptional", "People self-correct, ask for clarification, repeat, reformulate, and resolve reference. Robust assistants need explicit states for uncertainty, confirmation, correction, and rollback rather than one-shot intent classification."],
    ],
    worked: {
      title: "Repairing a dangerous ambiguous request",
      text: "User: “Delete the old one.” Assistant: “Do you mean the staging database snapshot from July 12, not the production snapshot?” User: “Yes, staging only.” The first turn is an underspecified request. The assistant initiates repair and presents a grounded candidate with a contrast. The confirmation establishes common ground before action. A good evaluation checks that the system selected the right object, asked at the right risk threshold, recorded consent, and verified the result—not merely that the wording sounded polite.",
    },
    pitfalls: [
      "Treating each turn as independent and losing corrections or commitments.",
      "Mapping literal sentence form directly to dialog act.",
      "Using excessive confirmation that makes safe tasks unusable.",
      "Evaluating with static single-turn benchmarks only.",
      "Optimizing engagement when the correct behavior is completion, refusal, or handoff.",
    ],
    practice: [
      "Annotate dialog acts in a real support conversation.",
      "Mark adjacency pairs, repairs, and grounding evidence.",
      "Design a conversation state that records commitments and unresolved references.",
      "Create interruption, correction, and topic-shift tests.",
      "Measure task success, user effort, recovery, and unsafe-action rate.",
    ],
    faq: [
      ["What is common ground?", "Information participants treat as mutually known or accepted for the purpose of the current interaction."],
      ["Why are confirmations sometimes harmful?", "Confirming every low-risk step adds friction. Systems should use uncertainty and action cost to choose when explicit confirmation is worth it."],
      ["What makes conversation different from question answering?", "Conversation carries state, roles, commitments, repair, timing, and social expectations across multiple turns."],
    ],
    takeaways: [
      "Conversation is coordinated action across turns.",
      "Dialog acts describe function rather than surface grammar.",
      "Adjacency pairs and grounding organize expectations.",
      "Repair is a core capability for safe interaction.",
      "Success includes outcomes, effort, recovery, and safety.",
    ],
  },
];
