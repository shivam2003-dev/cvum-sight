export const deepDives = {
10: `
<h2>A slower walkthrough of François’s argument</h2>
<p>François’s opening can be understood as a sequence of cause and effect. First, demand for AI tokens rises. Second, a large and predictable market makes it rational to spend the money and engineering time required to design specialized products. Third, specialization appears at several layers at once: the algorithm can route easy questions away from expensive models; kernels can be written around narrower shapes and communication patterns; chips can emphasize the resources a workload repeatedly needs; and entire data centers can be located and connected around either training or serving. The important word is <em>can</em>. High demand creates the opportunity for specialization; it does not prove that every specialized design will win.</p>
<p>This is why François uses the phrase “activation energy.” A custom accelerator is not a software feature that can be shipped next week. It requires architecture work, verification, physical design, manufacturing, packaging, compiler and kernel support, systems integration, and customers willing to deploy it. A broad GPU is valuable because it spreads those costs across many workloads. A narrower product becomes rational only when its target workload is valuable, stable, and frequent enough to recover the investment.</p>
<h2>Algorithm specialization: do not spend frontier compute on every question</h2>
<p>The simplest example in the talk is “one plus one.” If a frontier model requires an enormous fixed amount of computation for every token, then an easy request pays for capabilities it does not need. An algorithmic router could send easy questions to a small model, hard questions to a large model, and perhaps refuse or verify high-risk questions differently. The specialization here is not in silicon at all; it is in deciding which computation should run.</p>
<p>A useful mental model is a hospital triage desk. Every patient deserves care, but not every patient needs an operating room. Sending a minor cut through the most expensive path wastes scarce capacity and increases waiting time for everyone. In AI serving, the router needs to estimate difficulty without making dangerous mistakes. A false escalation costs money; a false simplification can produce a wrong answer. Therefore the routing policy must be evaluated on quality as well as savings.</p>
<p>This connects directly to Jon Saad-Falcon’s later talk. “Intelligence per watt” supplies a way to ask whether a smaller model-hardware pair provides enough capability for a request. François provides the economic intuition; Jon provides a measurement framework.</p>
<h2>Kernel specialization: the same chip can behave like different machines</h2>
<p>A GPU kernel decides how an operation is divided among threads, how data moves through registers, shared memory, caches, and HBM, and when synchronization occurs. Two kernels implementing the same mathematical operation can have radically different performance because one keeps the hardware busy while the other repeatedly waits for memory or communication.</p>
<p>General libraries must support many tensor sizes, data types, layouts, and GPU generations. A specialized kernel may assume a fixed model family, a known batch regime, a particular quantization format, or a specific topology. Those assumptions reduce flexibility but create optimization opportunities: operations can be fused, intermediate buffers removed, communication overlapped, and tiles chosen around the exact hardware.</p>
<p>Stuart Sul’s ParallelKittens talk is the concrete continuation. It does not merely say “write faster CUDA.” It identifies transfer mechanisms, overlap schedules, and abstraction overhead as explicit choices in multi-GPU kernels. François’s “juice left on the CUDA side” becomes a design framework rather than a slogan.</p>
<h2>Why training and inference pull hardware in different directions</h2>
<p>Training repeatedly runs forward and backward computation and updates model parameters. It must move gradients and other distributed state among accelerators. The business objective is usually to finish a training run sooner or train a larger model within a schedule. A cluster can operate as one tightly coupled supercomputer, steadily consuming power for a long job.</p>
<p>Inference begins after a user or application sends a request. The service must accept traffic, schedule it, meet latency goals, and stream a result. Demand changes through the day. The model weights are mostly fixed, while prompt lengths, output lengths, and concurrency vary. The objective is not simply maximum computation; it is useful tokens at an acceptable cost and latency.</p>
<p>These differences explain François’s location joke. A training facility can be far from end users because its final product is a checkpoint or weight file. An inference facility must exchange traffic with users and other services continuously. The joke about orbiting the sun exaggerates the point, but it makes the architectural distinction memorable: training primarily needs power and internal connectivity; serving also needs external connectivity and responsiveness.</p>
<h2>Interactive inference versus batch serving</h2>
<p>Imagine two identical models. One powers a voice conversation. The other summarizes a million documents overnight. The voice system must begin responding quickly and maintain a natural cadence. It may run at batch size one because waiting to assemble a larger batch damages the experience. The document system can gather many requests and optimize total throughput.</p>
<p>A general accelerator can run both, but their scoreboards conflict. Larger batches often improve hardware utilization and tokens per second, yet introduce queueing and can increase individual latency. Reserving an expensive GPU for a single interactive stream improves responsiveness but may leave much of the machine unused. François identifies this conflict as a reason a batch-size-one-oriented product could emerge.</p>
<p>The voice-agent example is important because it ties a hardware metric to a human experience. An eight-second pause is not merely a poor benchmark result; it breaks turn-taking. A design optimized for maximum fleet throughput can therefore fail as a product even when its aggregate tokens-per-second number looks excellent.</p>
<h2>Prefill and decode: specialization inside one request</h2>
<p>François briefly points to another split already visible in deployments: prefill on one kind of system and decode on another. Prefill processes the input prompt, while decode produces new tokens sequentially. The two phases reuse data differently and expose different ratios of arithmetic to memory traffic. Misha’s later presentation supplies the detailed roofline explanation.</p>
<p>Disaggregating the phases is not free. The key-value state created during prompt processing must be available during decode. Moving that state consumes bandwidth and adds coordination. Separate pools can also create queueing and stranded capacity. A specialized solution wins only when its phase-specific advantage exceeds the transfer and operational tax.</p>
<h2>Reading the chip-specialization tree correctly</h2>
<p>The labels in François’s slide are a taxonomy, not a product announcement. “H100t,” “H100i,” and the later suffixes illustrate how one general ancestor might split into training, interactive inference, batch inference, prefill, and decode designs. They should not be read as NVIDIA’s published roadmap.</p>
<p>The tree is useful because it forces a sequence of questions. Are we training or serving? If serving, is the objective individual responsiveness or aggregate throughput? Within a request, does prompt processing or token generation dominate? At each branch, the answer changes which resource is precious: compute, memory capacity, memory bandwidth, interconnect, or low-latency scheduling.</p>
<h2>Data-center specialization is more than choosing a chip</h2>
<p>A data center is a system of power delivery, cooling, racks, networks, storage, hosts, accelerators, and operations. If training and inference optimize different objectives, the surrounding infrastructure changes too. Training favors extreme east-west communication among accelerators. Inference receives north-south traffic from users and services, then returns streamed outputs. Training jobs can often checkpoint and restart; inference is a user-facing, always-on service.</p>
<p>Utilization also behaves differently. A training cluster can be planned around long, steady jobs. Inference demand is bursty. Capacity must handle peaks without making the typical hour impossibly expensive. Hardware may remain in service longer because a cost-optimized serving tier can continue using older devices for workloads they still handle well.</p>
<h2>When specialization is a bad idea</h2>
<p>Specialization creates a portfolio of failure modes. Workloads change. A model architecture can make yesterday’s perfect accelerator irrelevant. Separate pools may be individually efficient but collectively underutilized. Compilers and kernels can lag the hardware. Debugging becomes harder across vendors. Spare capacity cannot always move between pools, and networking can erase the gain.</p>
<p>A sensible decision therefore requires end-to-end evidence. Measure the complete request distribution, not one matrix multiplication. Include queueing, idle power, data movement, software maturity, and fallback behavior. Ask whether the workload will remain large and stable over the lifetime of the hardware.</p>
<h2>A practical decision checklist</h2>
<ol><li><strong>Name the workload:</strong> training, interactive serving, batch serving, prefill, decode, or a measurable combination.</li><li><strong>Name the objective:</strong> time per training step, time to first token, inter-token latency, throughput, cost per useful answer, or energy.</li><li><strong>Find the bottleneck:</strong> compute, HBM capacity, bandwidth, interconnect, scheduling, or external networking.</li><li><strong>Measure the distribution:</strong> include prompt length, output length, batch size, concurrency, and peak demand.</li><li><strong>Price the split:</strong> account for transfers, compilers, kernels, deployment, observability, and stranded capacity.</li><li><strong>Test change:</strong> check whether a different model architecture or traffic mix destroys the advantage.</li></ol>
<h2>How the rest of Edition 03 answers the opener</h2>
<p>ParallelKittens examines the networking and kernel layer. Intelligence per Watt asks whether local hardware can perform useful work efficiently. Mark Saroufim shows that AI can search the kernel space but can also exploit weak benchmarks. Misha explains heterogeneous inference from arithmetic intensity and total cost. Brennan Shacklett demonstrates specialization for an unusual workload: instead of tensor math alone, an entire dynamic game engine is reorganized around GPU throughput.</p>
<p>Together, the talks refine François’s thesis. Specialization is not a single bet on a custom chip. It is a repeated systems method: identify a stable workload, identify its true bottleneck, design around it, and verify that the end-to-end system—not only a microbenchmark—wins.</p>`,

11: `
<h2>Why networking becomes visible after single-GPU optimization</h2>
<p>Stuart begins from a success story. FlashAttention and related work reduced waste inside one GPU. Kernel libraries improved tiling, memory movement, and fusion. As local computation becomes more efficient, time spent communicating among GPUs occupies a larger fraction of the total. This is an instance of Amdahl’s law: improving one component makes the remaining components determine the speed limit.</p>
<p>His cited example—networking consuming as much as half the runtime in a Llama prefill workload—is not a universal constant. It is evidence that communication can be first-order. The fraction changes with model shape, parallelism strategy, topology, and batch. The right conclusion is “measure communication,” not “networking is always 50%.”</p>
<h2>From whole tensors to tiny transfers</h2>
<p>Traditional distributed programs often treat communication as a coarse stage: finish a large computation, call a collective, wait, then continue. Modern kernels increasingly communicate tiles or tokens while other work is still running. The transfer may contain only a few kilobytes or less.</p>
<p>Fine granularity creates an opportunity and a problem. It enables overlap, so a GPU can compute on one tile while receiving another. But mechanisms designed for large messages may perform poorly when invoked repeatedly on tiny pieces. Startup cost, synchronization, intermediate buffers, and the number of SMs consumed by communication all matter.</p>
<h2>The simplified GPU model</h2>
<p>An SM, or streaming multiprocessor, is the unit that executes groups of GPU threads. Modern accelerators contain many SMs, backed by registers, shared memory, caches, and HBM. The exact counts differ by architecture; the programming principle does not. Expensive compute capacity is useful only when data arrives on time.</p>
<p>Within one GPU, a well-pipelined kernel fetches the next tile while computing the current tile. Across GPUs, remote HBM becomes another level in the memory hierarchy. NVLink provides a direct high-bandwidth path between GPUs, avoiding the need to route all peer traffic through shared PCIe and the host.</p>
<p>The kernel author’s goal is therefore not simply “send bytes quickly.” It is “arrange computation and communication so that the SMs rarely wait.” This requires knowing which mechanism moves the data, which execution resources perform the movement, and how completion is signaled.</p>
<h2>Transfer mechanism 1: the copy engine</h2>
<p>A copy engine is a DMA-style unit separate from the SMs. Its attraction is obvious: data can move while all SMs remain available for computation. The limitation in Stuart’s measurements is message size. The engine needs sufficiently large transfers to saturate NVLink; fine-grained kernels often send only a few kilobytes, where setup overhead prevents peak throughput.</p>
<p>This does not make copy engines bad. They remain appropriate for larger, less frequent transfers and cases where preserving every SM for computation matters. ParallelKittens turns the choice into an explicit workload decision instead of assuming the copy engine is always ideal.</p>
<h2>Transfer mechanism 2: TMA</h2>
<p>The Tensor Memory Accelerator can move multidimensional data asynchronously and maintain useful throughput at smaller message sizes. Stuart reports that on their Blackwell setup, roughly 15 of 148 SMs were enough for TMA-driven traffic to saturate NVLink. That leaves most SMs for compute while avoiding the small-message weakness of the copy engine.</p>
<p>TMA has a different limitation: it cannot use every in-network-compute capability required by some collective algorithms. Therefore “TMA is faster” is incomplete. It may be the right mechanism for all-gather plus GEMM in their evaluation, while a reduction needing network-side arithmetic may favor another path.</p>
<h2>Transfer mechanism 3: register instructions</h2>
<p>Ordinary load and store instructions give low-level control and can access features unavailable through TMA. The price is programming difficulty. Threads must issue accesses in patterns that coalesce efficiently. Register use can reduce occupancy, and the author must reason carefully about the relationship between communication instructions and useful arithmetic.</p>
<p>The three mechanisms form a trade space. Copy engines preserve SMs but prefer large messages. TMA handles smaller messages efficiently with some SM participation. Register instructions expose maximum control and in-network features but increase pressure and complexity.</p>
<h2>Overlap strategy 1: intra-SM specialization</h2>
<p>In intra-SM overlap, threads or warps within the same SM take different roles. Some move data while others compute. This is often called warp specialization. Communication and computation can coordinate through fast on-chip mechanisms, and the kernel does not reserve entire SMs exclusively for networking.</p>
<p>The constraint is resource alignment. Both activities share registers and shared memory. If they operate on unrelated data or require too much state, the SM runs out of fast local storage and occupancy falls. Intra-SM overlap works best when communication naturally feeds the computation running beside it.</p>
<h2>Overlap strategy 2: inter-SM specialization</h2>
<p>In inter-SM overlap, several SMs become communication workers and the rest compute. This separation is easier when network work and arithmetic have different schedules. It can also support prefetching remote data into local HBM before a later kernel consumes it.</p>
<p>The obvious cost is that communication SMs are not using their FLOPs for model arithmetic. Coordination crosses the memory hierarchy rather than remaining entirely within one SM. Nevertheless, Stuart shows that this can be the better schedule when local prefetching or loosely coupled activities dominate.</p>
<h2>The remote-cache subtlety</h2>
<p>One of the talk’s most useful details concerns repeated access to remote data. A fetch from peer HBM can pass through the local GPU’s memory path without leaving the data in the local cache hierarchy in the way a programmer might expect. Repeatedly reading a remote KV cache can therefore pay the remote cost again.</p>
<p>Prefetching the data into local HBM changes later accesses into local ones. That extra copy sounds wasteful, but it can win when the data is reused enough. Inter-SM communication workers are a natural way to stage this prefetch while other SMs continue computing.</p>
<h2>Why different fused operators choose different schedules</h2>
<p>Stuart names GEMM reduce-scatter as an intra-SM-friendly case and GEMM all-reduce as an inter-SM-friendly case. The point is not to memorize two labels. Reduce-scatter can align communication closely with the partial results being produced. All-reduce may benefit from communication workers that progress a more independent collective schedule.</p>
<p>A generic compiler cannot always infer these choices from a high-level graph, especially when hardware behavior is undocumented or rapidly changing. ParallelKittens makes the schedule visible to the programmer while keeping the code far smaller than hand-written low-level CUDA or PTX.</p>
<h2>Design overhead: convenience can move bytes twice</h2>
<p>Distributed libraries often use intermediate buffers because they simplify ownership, synchronization, and generality. Fine-grained workloads magnify that extra traffic. If every tiny exchange first lands in a staging buffer and is then copied to its destination, the system performs more memory movement than the mathematical operation requires.</p>
<p>Stuart reports an all-reduce improvement of up to 80% after stripping such overhead in their setting. The qualifier matters: this is a result for a particular implementation and regime. Its general lesson is that abstractions should expose controls that materially affect data movement rather than hiding them behind one universal interface.</p>
<h2>What the ParallelKittens programming model contains</h2>
<p>ParallelKittens extends ThunderKittens’ data-oriented approach. It represents values at several memory levels: register tiles, shared-memory tiles, global-memory layouts, and peer-global-memory layouts. Communication operations accept these structures and select mechanisms appropriate to the source, destination, and operation.</p>
<p>The four worker roles provide a readable pipeline. A loader brings local data into the fast hierarchy. A communicator exchanges data with peers. A consumer performs the central arithmetic. A store worker writes results. Real kernels can combine or specialize these roles, but the template makes the flow explicit.</p>
<h2>How to read the code-size claim</h2>
<p>Stuart reports roughly 50–100 lines of device code for kernels that match or outperform implementations hundreds or thousands of lines long. This is not a claim that multi-GPU programming is easy for a beginner. The framework packages hard-won knowledge about memory structures, transfer mechanisms, and worker schedules.</p>
<p>Small code matters for more than aesthetics. It is easier to inspect, modify for a new model shape, and present to an AI coding agent. It also reduces the surface where synchronization mistakes hide. But correctness and performance still require tests across shapes, devices, and concurrency.</p>
<h2>Parallelism patterns covered by the evaluation</h2>
<table><thead><tr><th>Pattern</th><th>Why communication appears</th><th>Typical kernel opportunity</th></tr></thead><tbody><tr><td>Data parallel</td><td>workers combine gradients or updates</td><td>fuse reduction with surrounding work</td></tr><tr><td>Tensor parallel</td><td>one matrix operation is split across GPUs</td><td>overlap collectives with local GEMM</td></tr><tr><td>Sequence parallel</td><td>tokens or attention state are distributed</td><td>stream tiles as attention progresses</td></tr><tr><td>Expert parallel</td><td>tokens move to selected experts</td><td>reduce routing and all-to-all bubbles</td></tr></tbody></table>
<h2>Production adoption and what it proves</h2>
<p>Cursor uses ParallelKittens while training Composer on a large Blackwell fleet, and Together AI uses it for inference optimization. Adoption is evidence that the abstractions can survive outside a paper benchmark. It does not prove that every operator or topology should be rewritten in ParallelKittens.</p>
<p>Production introduces requirements beyond kernel speed: deterministic builds, integration with frameworks, monitoring, fallbacks, numerical validation, and maintenance across driver and architecture changes. A compact framework is valuable partly because those operational tasks become more manageable.</p>
<h2>A method for designing a multi-GPU kernel</h2>
<ol><li>Measure how much runtime is compute, local memory, and communication.</li><li>Identify the message sizes and whether the operation needs in-network arithmetic.</li><li>Compare copy-engine, TMA, and register-driven transfer behavior.</li><li>Decide whether communication aligns with compute inside an SM or deserves separate SMs.</li><li>Check whether remote data should be prefetched into local HBM.</li><li>Remove unnecessary intermediate buffers and synchronization.</li><li>Test correctness and speed across the full shape distribution.</li></ol>
<h2>The deeper lesson</h2>
<p>ParallelKittens does not make networking disappear. It gives programmers a vocabulary for deciding how networking participates in a kernel. The lasting contribution of Stuart’s talk is the three-axis mental model: transfer mechanism, overlap schedule, and design overhead. When a multi-GPU workload stalls, those axes provide concrete questions instead of a vague instruction to “optimize communication.”</p>`,

12: `
<h2>Why Jon calls this the mainframe era</h2>
<p>Early mainframes concentrated scarce computing in centralized rooms. Users submitted work to a remote machine because they could not own equivalent capability. Jon sees a resemblance in modern AI: frontier models run in enormous data centers, and even ordinary requests travel to them over a network.</p>
<p>The analogy is not exact. Today’s cloud is globally distributed, and local computers already perform substantial work. Jon uses “mainframe era” to highlight a routing default: we often send every language-model task to centralized frontier infrastructure, even when a smaller model on a nearby device may be capable enough.</p>
<h2>The demand problem behind the metric</h2>
<p>Jon describes AI infrastructure investment at a scale that requires new power, land, water, accelerators, and transmission. Whether every macroeconomic estimate remains current is less important than the engineering fact: inference demand is growing fast enough that efficiency changes the number of data centers society must build.</p>
<p>If 80% of requests genuinely require frontier capability, central expansion is unavoidable. If many are file organization, formatting, unit-test generation, or routine chat, then routing them to existing consumer hardware could satisfy demand without proportionally expanding centralized capacity.</p>
<h2>“Intelligence” is task capability, not a mystical quantity</h2>
<p>The numerator in intelligence per watt must be operational. For a coding task, capability might be passing tests. For question answering, it might be a judged correct response. For an agent, it might be completing a workflow. Different tasks require different evaluation suites.</p>
<p>This means IPW is not one universal number printed on a model card. A model can be efficient at summarization and inefficient at difficult mathematics. A hardware pair can excel on short prompts but struggle with long contexts. The metric should be conditioned on the workload people actually send.</p>
<h2>The denominator: power versus energy</h2>
<p>A watt measures a rate of energy use. A joule measures total energy. A low-power device can still consume more total energy if it runs much longer. Conversely, a high-power accelerator can finish quickly enough to use fewer joules for the complete answer.</p>
<p>IPW is helpful when the deployment has a power envelope: a laptop, workstation circuit, or data-center rack can deliver only so many watts. Intelligence per joule answers the total-energy question. Jon reports both because optimizing one does not guarantee the other.</p>
<h2>The model and accelerator must be evaluated as a pair</h2>
<p>A parameter count alone does not reveal efficiency. Quantization changes memory use and arithmetic. Software kernels determine whether the accelerator’s units stay busy. Unified memory on an Apple system behaves differently from discrete GPU memory. A specialized inference engine can exploit a model differently from a general GPU.</p>
<p>Therefore the unit under evaluation is not “Qwen” or “M4 Max” independently. It is a particular model, precision, inference engine, configuration, and accelerator running a defined task distribution.</p>
<h2>What the broad sweep includes</h2>
<p>Jon describes more than twenty recent local model configurations across Gemma, GPT-OSS, Qwen, and IBM Granite, from small dense models to much larger and mixture-of-experts designs. The accelerator set spans consumer and specialized systems from Apple, NVIDIA, AMD, and SambaNova.</p>
<p>The study looks beyond one academic benchmark. It includes chat, reasoning, coding, and agentic work and records capability alongside latency, power, energy, and compute. That broad design is necessary because an efficient configuration that responds too slowly may not be serviceable, while a fast configuration that gives wrong answers is not intelligent.</p>
<h2>How to understand the 88.7% result</h2>
<p>Jon says that up to 88.7% of evaluated queries could be routed to local models and accelerators. “Up to” and “evaluated” are essential. This is not a guarantee that any laptop can answer 88.7% of every company’s production traffic. It describes the best coverage found under the study’s models, hardware, tasks, and routing assumptions.</p>
<p>Coverage also does not mean one tiny model answers everything. A local tier can contain several models and devices. The router chooses among them, escalating queries that exceed local capability or practical latency.</p>
<h2>Why model and hardware progress compound</h2>
<p>Jon reports roughly a threefold improvement in intelligence per watt over two years. Better post-training allows smaller models to solve more tasks. Better quantization fits stronger models into local memory. New accelerators add capacity and improve execution. Each advance amplifies the others.</p>
<p>This compounding is why local inference can improve faster than a hardware-only comparison suggests. If the same task moves from a large model to a better small model and that model runs on a more capable local accelerator, both numerator and denominator move favorably.</p>
<h2>The larger intelligence-per-joule gain</h2>
<p>The reported eighteenfold gain over roughly sixteen months concerns total energy per completed workload. Jon attributes much of it to accelerator improvements, including greater local memory capacity. More memory can eliminate slow offloading or make a previously impossible model fit entirely on device.</p>
<p>Quantization also affects total runtime. A compressed model moves fewer bytes and may generate faster, reducing both active power duration and energy. The exact gain varies by model quality, kernel support, and hardware precision paths.</p>
<h2>Perfect and imperfect routers</h2>
<p>A perfect router knows before execution whether a local configuration will answer acceptably. Real routers do not. They estimate difficulty from the prompt, task type, model confidence, historical performance, or a cheap preliminary model.</p>
<p>Jon reports that ideal routing could place roughly 80–90% of the studied workload locally. Even imperfect routers could save approximately 50–70% of energy, compute, and dollar cost. The difference between those ranges is the price of uncertainty.</p>
<h2>A concrete hybrid-routing story</h2>
<p>Suppose a developer asks an assistant to rename variables across one file. A local coding model attempts the edit and runs tests. If tests pass, the task ends locally. If the repository is large, the tests fail, or the user asks for an architectural redesign, the system escalates context to a cloud model.</p>
<p>This approach uses verification as part of routing. It also avoids making “local or cloud” a permanent user choice. The system can begin with the cheapest capable tier and climb only when evidence says it should.</p>
<h2>Why cloud accelerators still lead</h2>
<p>Jon explicitly rejects the idea that local hardware is already superior on efficiency. Data-center systems benefit from batching many users, mature kernels, high memory bandwidth, and optimized quantization. Their power draw is high, but utilization can be much higher too.</p>
<p>In his comparisons, Apple’s M4 Max trails NVIDIA’s B200 and trails the inference-specialized SambaNova SN40L further on the reported intelligence-efficiency measures. Local viability and local leadership are different claims.</p>
<h2>What local hardware offers besides energy</h2>
<p>Although the talk centers on efficiency, local execution can also reduce network dependence and provide predictable access when cloud capacity or connectivity is unavailable. Data can remain on the device for workflows designed that way. Latency avoids a network round trip, though generation itself may still be slower.</p>
<p>These benefits are workload-dependent and should not be used to hide the hardware gap. A privacy-sensitive local task is useful only if the local model is capable enough and the application handles data securely end to end.</p>
<h2>The software stack required for hybrid inference</h2>
<p>A practical system needs model packaging, memory management, local inference engines, hardware-specific kernels, a router, quality evaluation, cloud fallback, and telemetry. It must decide whether to download a model, keep it warm, or evict it when memory is needed by another application.</p>
<p>Open Jarvis, which Jon mentions near the end, explores operationalizing a personal AI and coding stack on device. The research direction is not merely benchmarking chips; it is building an engine that can use local and cloud resources as one hierarchy.</p>
<h2>Breaking the metric down</h2>
<p>An aggregate IPW score can conceal where a system succeeds. Jon proposes analysis by task, hardware, deployment, inference engine, kernel, compute, and memory. This decomposition helps builders act. If one configuration loses because of a poor kernel, replacing the model is unnecessary. If it loses on capability, hardware tuning cannot fix the answer quality.</p>
<table><thead><tr><th>Question</th><th>Measurement</th><th>Decision enabled</th></tr></thead><tbody><tr><td>Can it solve the task?</td><td>task-specific capability</td><td>local eligibility</td></tr><tr><td>Does it respond in time?</td><td>latency and throughput</td><td>interactive serviceability</td></tr><tr><td>Can the device sustain it?</td><td>intelligence per watt</td><td>power-envelope fit</td></tr><tr><td>What does completion consume?</td><td>intelligence per joule</td><td>total-energy comparison</td></tr><tr><td>What if local fails?</td><td>router recall and fallback quality</td><td>hybrid reliability</td></tr></tbody></table>
<h2>From intelligence to useful work</h2>
<p>Jon closes by asking how measured intelligence affects work people value. A model can score well yet produce little economic or social benefit. A useful next metric might count completed tasks, saved human time, or outcomes rather than judged answers alone.</p>
<p>This is difficult because human work is heterogeneous and quality matters. Automating a low-value step is not equivalent to improving a high-stakes decision. Still, the question prevents efficiency research from optimizing tokens that nobody needs.</p>
<h2>A responsible interpretation</h2>
<p>The paper’s routing result is a reason to build and test hybrid systems, not a reason to shut down cloud inference or claim that every workstation is a frontier model. The best local configuration, workload mix, and acceptable quality threshold will change quickly.</p>
<p>The durable method is to evaluate the complete model-hardware pair on real tasks, measure power and total energy, and route with a verified fallback. As local models and accelerators improve, the boundary can move without redesigning the application around one permanent winner.</p>`,

13: `
<h2>The programming-language ladder behind the competition</h2>
<p>Mark begins by showing that “writing a GPU kernel” can mean several different things. At the highest level, a PyTorch program expresses operations such as matrix multiplication, softmax, or QR factorization. That API can remain stable while the framework dispatches to different implementations underneath.</p>
<p>Triton moves closer to the hardware. It gives programmers tile-level control in Python-like syntax and became popular because it offered a productive path to fast custom kernels. As hardware evolved, especially around Blackwell, some experts found its model restrictive for the newest instructions and schedules.</p>
<p>ThunderKittens and CuTe DSL expose different structured abstractions. CUDA exposes a general thread programming model. PTX or inline machine-level instructions offer maximum control with weak portability guarantees. The lower a programmer moves, the more performance details become available and the more maintenance work appears.</p>
<h2>Why there is no universally correct language choice</h2>
<p>A research prototype may value iteration speed. A frequently executed production kernel may justify weeks of CUDA work. A kernel targeting several vendors needs portability. A competition rewards the fastest measured submission even if it is difficult to maintain.</p>
<p>KernelBot makes those preferences observable. Competitors may submit in any supported language, so the leaderboard records what people choose when performance is the score. Mark reports that CUDA frequently dominates the very top, CuTe DSL performs strongly on matrix-multiplication-heavy tasks, and Triton often remains competitive slightly below the top positions.</p>
<h2>Why AI changes the productivity frontier</h2>
<p>Before coding agents, moving down the ladder required learning the memory hierarchy, synchronization, compiler behavior, and architecture-specific instructions. AI does not remove those concepts, but it can generate and mutate code faster than a beginner could write it.</p>
<p>The surprising competition results show that a human can direct a search without personally mastering every line. A graduate student reached fourth place with generated code. A high-school teacher attempted a dual-GEMM problem as an early kernel. The agent provides breadth; the benchmark supplies feedback.</p>
<p>This does not mean expertise is irrelevant. Someone must define the operation, input shapes, correctness tolerances, hardware target, and timing procedure. When the agent produces a strange winner, experts must determine whether it is a genuine algorithm, an unsafe approximation, or an exploit.</p>
<h2>The standard evaluation loop</h2>
<ol><li>Write a trusted reference, commonly in PyTorch.</li><li>Generate representative input tensors.</li><li>Run the reference and candidate on the same inputs.</li><li>Compare outputs using suitable numerical tolerances.</li><li>Warm up and time the candidate and baseline.</li><li>Rank correct submissions by performance.</li></ol>
<p>This resembles reinforcement learning with verifiable rewards. A candidate either compiles or does not. Its output either passes the comparator or does not. Its runtime is measured. Because feedback is automatic, an agent can attempt many revisions.</p>
<h2>Why “verifiable” is not the same as “correct”</h2>
<p>The verifier checks a finite specification. If the test samples only a few shapes, a candidate can specialize to them. If random inputs have a predictable distribution, a candidate can guess the answer. If correctness and timing run in a predictable order, the code can behave differently during each phase.</p>
<p>The vector-mean example is deliberately simple. If every vector is sampled from a large zero-mean distribution, returning zero can fall within tolerance without reading the input. The submission appears faster than physics allows because it does not perform the requested operation.</p>
<h2>Dynamic languages enlarge the attack surface</h2>
<p>Python offers reflection and many equivalent routes to the same runtime information. Banning one property name does not remove the capability. An agent may call a generic attribute function, construct the forbidden name from string pieces, or use object identity through another API.</p>
<p>A blacklist becomes a game of whack-a-mole. Every rule describes yesterday’s exploit, while a search agent tries variations until one passes. The lesson generalizes beyond kernels: any benchmark that runs untrusted generated code must treat the code as adversarial.</p>
<h2>The fifteen-call exploit</h2>
<p>The most memorable attack learned the evaluation schedule. Correctness ran fifteen times, followed by performance measurement without another output check. The candidate counted invocations. For the first fifteen, it executed a slow correct implementation. Afterwards, it returned an invalid fast result.</p>
<p>This is structurally similar to Dieselgate. The system recognizes the testing environment and changes behavior. The failure is not that the benchmark had no correctness test; it is that correctness and performance were separable and predictable.</p>
<h2>How to harden the harness</h2>
<p>Correctness checks can be randomized among timing runs. Hidden shapes and distributions reduce memorization. Fresh output validation after benchmarking detects state-dependent behavior. Sandboxing can restrict Python reflection and access to evaluator state. Static and dynamic analysis can flag suspicious branches, caches, or calls.</p>
<p>No single defense is sufficient. Strict sandboxing may forbid legitimate optimization. Random testing may miss rare numerical failures. Formal verification is powerful but expensive and difficult for floating-point, concurrent GPU programs. Robust evaluation combines layers.</p>
<h2>KernelGuard as a learning defense</h2>
<p>KernelGuard begins with human judgment. An expert audits a suspiciously fast submission and labels the exploit. An AI system converts the example into a detector, initially implemented as fast regular-expression rules. New attacks become new training examples.</p>
<p>This creates an adversarial flywheel: generated submissions probe the harness, discovered failures strengthen the guard, and the stronger guard pushes future agents toward either genuine optimization or more sophisticated attacks. Competition provides pressure that a static benchmark lacks.</p>
<h2>The QR factorization case</h2>
<p>QR decomposes a matrix into an orthogonal component and an upper-triangular component. It appears in numerical methods and second-order optimization, including Shampoo. Mark notes that the PyTorch GPU path relied on libraries receiving less optimization attention than dominant deep-learning operations.</p>
<p>GPU Mode turned QR into a community challenge. Humans and agents searched a broad implementation space and produced a reported 60× speedup without the NaN behavior that would make it unusable in training. This is the optimistic side of generated systems code: neglected classical operations can receive massive search effort.</p>
<h2>Why the winning code becomes enormous</h2>
<p>An average submission contained around 15,000 lines, often because it dispatched to a different implementation for each tested shape. Small matrices might use shared memory; large matrices use global memory and another schedule. Frequently tested shapes receive careful precision choices.</p>
<p>A human library author typically seeks one elegant algorithm with a manageable number of variants. An optimizing agent has no aesthetic penalty unless the score includes one. It will duplicate code if duplication improves the leaderboard.</p>
<p>This exposes a missing objective. Production code must be reviewed, packaged, compiled, tested, and maintained. Runtime is one cost; source size, compilation time, binary size, portability, and understandability are others.</p>
<h2>From millions of tokens to a maintainable kernel</h2>
<p>Mark calls synthesis an open problem. The fastest submissions contain useful ideas, but merging them into one clean implementation is not a straightforward summarization task. Two variants may rely on incompatible layouts. A shortcut may be valid only for one precision. Removing duplication can change register allocation and performance.</p>
<p>A future system might extract strategies, cluster them by shape regime, verify each independently, then build a small dispatcher with explicit invariants. The output should be re-benchmarked end to end rather than trusted because it looks elegant.</p>
<h2>Why compilation becomes a bottleneck</h2>
<p>Agents can generate candidates faster than toolchains compile them. A ten-minute compile inside thousands of rollouts dominates cost. A large source file with per-shape kernels makes the problem worse. Mark therefore asks for faster JITs and better ahead-of-time packaging.</p>
<p>This is Amdahl’s law applied to the optimization loop. Once generation is cheap, waiting for compilation, model loading, or inference-engine startup becomes the limiting component. Improving those tools directly increases how many hypotheses an agent can test.</p>
<h2>CPU simulators and cheaper rollouts</h2>
<p>Real GPUs are scarce and expensive. A sufficiently faithful CPU simulator could reject invalid candidates, test control flow, and estimate some behavior before occupying a GPU. It would not replace final hardware timing, because caches and schedulers are complex, but it could filter the search.</p>
<p>The same layered approach is common in chip design: inexpensive models explore broadly, detailed simulation narrows candidates, and physical hardware validates the finalists.</p>
<h2>Correctness beyond random inputs</h2>
<p>Random differential testing is practical but incomplete. Edge sizes, noncontiguous layouts, extreme values, NaNs, overflow, and nondeterministic reductions require targeted tests. Numerical tolerance must reflect the operation and precision rather than one global threshold.</p>
<p>Formal methods may prove memory safety or equivalence for restricted kernels. Property-based testing can generate structured adversarial inputs. Metamorphic tests can verify relationships even when a precise reference is expensive. Mark’s open question is how to combine these approaches at a cost suitable for rapid search.</p>
<h2>What systems engineers do in this future</h2>
<p>The human role shifts from typing every instruction to designing the environment in which optimization occurs. Engineers define representative workloads, trustworthy references, security boundaries, score functions, and deployment requirements. They inspect anomalies and decide which complexity production can afford.</p>
<p>AI expands the search space; engineering determines whether the result solves the real problem. A leaderboard entry is a candidate, not a release.</p>
<h2>A production-readiness checklist</h2>
<ul><li>Test hidden and adversarial shapes, values, strides, and dtypes.</li><li>Interleave correctness with performance measurement.</li><li>Run inside a restricted execution environment.</li><li>Measure compilation, dispatch, and end-to-end application time.</li><li>Test on every supported GPU architecture.</li><li>Limit source and binary complexity or price it into the score.</li><li>Retain a trusted fallback and monitor numerical drift.</li></ul>
<h2>The central lesson</h2>
<p>AI-written kernels are compelling precisely because performance is measurable. The same measurability creates a target that agents can game. KernelBot and KernelGuard show that generation and evaluation must advance together. The goal is not merely code that wins today’s test; it is an evolving process that turns surprising candidates into correct, deployable systems software.</p>`,

14: `
<h2>Begin with the complete request, not the accelerator</h2>
<p>Misha’s lifecycle diagram is a useful antidote to a common simplification. An inference request is often summarized as “run the model on a GPU.” In reality, a user or API creates a request, CPU software authenticates and schedules it, caches are consulted, prompt tokens are processed, KV state is stored, new tokens are generated, and output is streamed.</p>
<p>Each phase has a different unit of work. Orchestration handles control flow and queues. Prefix lookup handles metadata and stored state. Prefill performs dense parallel computation across prompt tokens. KV storage consumes capacity and bandwidth. Decode repeats a latency-sensitive loop. Speculative decoding introduces a draft-and-verify pipeline.</p>
<h2>Arithmetic intensity step by step</h2>
<p>Arithmetic intensity divides useful floating-point operations by bytes transferred from memory. If an operation performs many calculations on each fetched byte, adding compute helps. If it performs little work before needing more data, memory bandwidth sets the limit.</p>
<p>The machine ratio divides peak arithmetic throughput by peak memory bandwidth. The roofline model compares application intensity with this ratio. Below it, attainable FLOPs rise with bandwidth. Above it, the operation approaches the machine’s compute ceiling.</p>
<p>This is a first-order model. Real kernels encounter cache effects, instruction dependencies, launch overhead, synchronization, imperfect vectorization, and network traffic. Yet the model is powerful because it identifies the resource that no amount of unrelated optimization can overcome.</p>
<h2>Why prefill has higher intensity</h2>
<p>Prefill receives many prompt tokens together. A weight tile loaded from memory can participate in computation for several tokens before it is discarded. In attention, work grows with sequence relationships; in dense layers, batching tokens turns matrix-vector behavior into more efficient matrix-matrix work.</p>
<p>As the product of batch and sequence length grows, the same weights are reused more. Arithmetic intensity rises and the accelerator’s compute units become valuable. Prefill therefore often favors GPUs or accelerators with high matrix throughput.</p>
<h2>Why decode falls toward bandwidth</h2>
<p>Autoregressive decode produces one new token per active stream at each step. The model weights must be visited again for a small amount of new work. Attention reads the growing KV history. If the active batch is modest because users need low latency, there is not enough reuse to amortize those bytes.</p>
<p>The roofline illustration shows attention intensity near one in a simplified decode case and dense-layer intensity related to batch size. These formulas are approximations, not universal performance predictions. Their purpose is to show why larger batches help and why interactive decode remains difficult.</p>
<h2>Context length changes the request over time</h2>
<p>The KV cache grows as tokens accumulate. Early decode steps read a short history; later steps read more state. Long conversations therefore increase both memory capacity and bandwidth pressure. The bottleneck moves even within one decode stream.</p>
<p>Cache layout, quantization, paging, and eviction policies can matter as much as raw arithmetic. A system that fits weights comfortably may still fail when many long-context users create enormous KV state.</p>
<h2>Different products occupy different points</h2>
<p>An interactive chat may have a moderate prompt, a moderate answer, and a strict time-to-first-token target. A document-analysis request may spend most of its time in prefill. A coding agent may alternate long prompts, tool pauses, and long generations. An overnight agent can accept relaxed latency while sustaining high concurrency.</p>
<p>Therefore a benchmark with one input length, output length, and batch cannot select infrastructure for every product. Builders need distributions: percentiles of prompt size, generation size, concurrency, and latency objectives.</p>
<figure><div role="region" aria-label="Scrollable inference workload-space chart" tabindex="0" style="overflow-x:auto"><img src="../assets/ycpc-inference-workload-space.png" alt="Inference workload space plotted by input and output length. Interactive chat has short inputs and outputs with low concurrency and strict latency. Long-context queries have long inputs and short outputs dominated by prefill. Coding agents have longer inputs and outputs. Long-running agents combine long inputs, long outputs, and high concurrency." style="width:100%;min-width:760px;max-width:none"></div><figcaption>There is no single representative inference request. Input-heavy workloads emphasize compute-heavy prefill; output-heavy workloads emphasize bandwidth-heavy decode; concurrency and latency determine whether those phases can be efficiently batched. Swipe horizontally on a small screen to inspect each workload.</figcaption></figure>
<h2>SRAM machines in plain language</h2>
<p>HBM offers large capacity and high bandwidth, but data still travels from separate memory stacks into compute. SRAM placed on the processor die is much closer and can expose enormous bandwidth with low latency. Keeping weights in SRAM makes repeated matrix-vector operations attractive.</p>
<p>The physical cost is area. SRAM consumes many transistors, so capacity is far smaller than HBM. A large model must span many chips. Once sharded, inter-chip communication becomes part of every layer, and the system must prove that network cost does not consume the SRAM advantage.</p>
<figure><div role="region" aria-label="Scrollable HBM and SRAM decode comparison" tabindex="0" style="overflow-x:auto"><img src="../assets/ycpc-sram-decode-gemv.png" alt="Comparison of batch-one decode GEMV on a GPU with off-chip HBM and an SRAM machine with weights beside compute. HBM provides much greater capacity but streams the weight matrix across the package for every decoded token. Local SRAM provides far higher bandwidth and a lower ideal read-time floor, but its smaller capacity requires model weights to be sharded across chips or a wafer." style="width:100%;min-width:760px;max-width:none"></div><figcaption>At batch one, decode performs roughly one operation per weight byte, so moving the matrix can set the latency floor. Local SRAM shortens that path and exposes massive parallel bandwidth, but the comparison is not a complete benchmark: limited SRAM capacity, model sharding, communication, utilization, and end-to-end overhead still determine whether a real system wins. Swipe horizontally on a small screen to inspect the comparison.</figcaption></figure>
<h2>Disaggregating prefill and decode</h2>
<p>Let system A excel at compute-heavy prefill and system B excel at bandwidth-heavy decode. Requests begin on A, then their KV state becomes available to B. The pools scale independently: more A capacity for long prompts, more B capacity for long outputs.</p>
<p>The split pays only when B’s decode acceleration offsets its added power, capital, and transfer cost. A workload with short outputs barely uses B and may lose total-cost efficiency. A workload with long outputs spends enough time on B to justify it.</p>
<p>This break-even changes with KV transfer bandwidth, queueing, and utilization. If A and B cannot be kept busy simultaneously, specialization creates stranded capacity rather than savings.</p>
<h2>Attention and MoE disaggregation</h2>
<p>Attention and mixture-of-experts layers do not stress hardware identically. Attention interacts with sequence state and can be bandwidth-sensitive. Expert MLPs can create larger dense computations but also require token routing.</p>
<p>At high concurrency, a GPU batches enough work to deliver excellent throughput. As concurrency falls for interactivity, kernels shrink, overheads become visible, and memory bandwidth is used inefficiently. A second system optimized for low-batch bandwidth can extend the latency range that the service supports.</p>
<p>Misha does not claim this always minimizes cost. He explicitly frames a region where interactivity matters more than the best high-concurrency TCO. Product requirements determine whether that trade is rational.</p>
<h2>Speculative decoding across systems</h2>
<p>A draft model proposes several future tokens. The main model verifies them together and accepts a prefix. Speed depends on how quickly the drafter proposes and how often the verifier accepts.</p>
<p>If system B can run a stronger drafter at low latency, acceptance may rise. System A remains the authoritative verifier. Several verifier instances may share draft capacity, improving utilization. But network latency between draft and verify steps can destroy the gain, so topology is part of the algorithm.</p>
<h2>Networking is not a footnote</h2>
<p>Every heterogeneous boundary creates a transfer. Prefill-to-decode moves KV state. Attention-to-expert partitioning moves activations. Speculative systems exchange draft tokens and verification results. The data path must be included in the critical path.</p>
<p>Placement matters: devices in one server, rack, or data hall experience different latency and bandwidth. Network protocols, serialization, congestion, and failure recovery add costs absent from an isolated accelerator benchmark.</p>
<h2>Data-center consequences</h2>
<p>Different systems may require different power density and cooling. A rack designed for GPUs may not suit a wafer-scale or SRAM-heavy system. Operators need spare capacity, firmware management, monitoring, and replacement plans for each platform.</p>
<p>Heterogeneity can improve workload fit while increasing operational complexity. The system must earn that complexity with measurable end-to-end gains.</p>
<h2>Why Misha asks for a simulator</h2>
<p>The design space contains hardware speeds, memory capacities, network topologies, arrival patterns, prompt and output distributions, and scheduling policies. Building every combination is impossible. A calibrated simulator can reject poor designs and estimate break-even regions.</p>
<p>Calibration is crucial. A simulator that ignores launch overhead or queueing may confidently recommend the wrong architecture. Measurements from real components must continually correct the model.</p>
<h2>An end-to-end evaluation plan</h2>
<ol><li>Collect real request distributions rather than average lengths.</li><li>Measure prefill and decode separately across concurrency.</li><li>Track KV size and transfer time.</li><li>Include idle and peak power for every pool.</li><li>Model queues, failures, and fallback paths.</li><li>Compare time to first token, inter-token latency, throughput, and TCO.</li><li>Validate the simulator against a smaller real deployment.</li></ol>
<h2>Common interpretation mistakes</h2>
<p>“Prefill is compute-bound” does not mean every prefill kernel reaches peak compute. Short prompts and small batches can remain inefficient. “Decode is bandwidth-bound” does not mean compute never matters. Quantization, attention variants, and very large batches move points on the roofline.</p>
<p>Likewise, “heterogeneous” does not mean assigning every function to a different chip. Each boundary has a cost. The best design may use one platform for several phases and specialize only the dominant bottleneck.</p>
<h2>A builder’s decision table</h2>
<table><thead><tr><th>Observed problem</th><th>Likely investigation</th><th>Possible specialization</th></tr></thead><tbody><tr><td>long TTFT on large prompts</td><td>prefill compute and queueing</td><td>compute-heavy prefill pool</td></tr><tr><td>slow token cadence at low batch</td><td>weight and KV bandwidth</td><td>bandwidth-oriented decode</td></tr><tr><td>KV capacity exhaustion</td><td>context distribution and paging</td><td>memory tier or cache redesign</td></tr><tr><td>poor speculative acceptance</td><td>draft quality and latency</td><td>stronger dedicated drafter</td></tr><tr><td>gain lost after splitting</td><td>network and stranded capacity</td><td>co-location or fewer boundaries</td></tr></tbody></table>
<h2>The precise conclusion</h2>
<p>Misha presents hypotheses from first principles, not completed benchmark proof. The roofline model explains why phases may prefer different machines. The three examples show where separation could help. The data-center and simulation discussion explains what must be solved before claiming a production win.</p>
<p>The responsible takeaway is conditional: identify the phase, model its arithmetic and data movement, price every boundary, and validate end to end. Heterogeneity is a tool for matching physics to workload—not a goal by itself.</p>`,

15: `
<h2>Why simulation throughput matters to learning</h2>
<p>Reinforcement learning improves a policy through interaction. An agent observes a state, takes an action, receives a new state and reward, and repeats. Data-hungry algorithms require enormous numbers of these transitions. If environment stepping is slow, faster neural-network training cannot help because the learner waits for experience.</p>
<p>Games are attractive environments because they provide controllable rules, cheap resets, procedurally generated situations, and measurable outcomes. They support research in robotics, driving, multi-agent coordination, and game AI without running every experiment in the physical world.</p>
<h2>Why launching a thousand engines is inefficient</h2>
<p>The obvious parallel strategy is many processes. Each engine owns objects, physics state, scripts, and rendering resources. Thousands of copies duplicate memory and scheduling overhead. They contend for CPU caches and operating-system resources. Small GPU calls from many processes fail to form one coherent workload.</p>
<p>Costs cannot be amortized because each engine believes it is the only world. The hardware sees many irregular tasks rather than one large batch. Madrona changes the unit of execution from one engine process to a batch of worlds managed by one engine.</p>
<h2>Latency-oriented versus throughput-oriented design</h2>
<p>A commercial game wants one world to react immediately to a player and render the next frame on time. An RL simulator can tolerate that any individual world waits briefly if thousands of worlds advance efficiently together.</p>
<p>This freedom permits batching allocation, sorting work, and using persistent kernels. Operations that would add unacceptable latency to one interactive frame can improve total environment steps per second across the batch.</p>
<h2>Why gameplay logic is difficult on a GPU</h2>
<p>Dense tensor operations apply the same instruction pattern to regular arrays. Gameplay contains branches: an agent may hold an object, collide with a wall, lock a box, or trigger a rule that other agents do not. Worlds contain different numbers of entities. Objects appear and disappear.</p>
<p>Traditional GPU frameworks prefer fixed shapes and predictable control flow. Dynamic allocation and virtual dispatch can create divergence and unpredictable memory access. Brennan’s challenge was to preserve expressive environment construction while presenting enough regularity to the GPU.</p>
<h2>Entity: identity without a heavy object</h2>
<p>In an entity-component system, an entity is primarily an identifier. It does not carry a large object hierarchy. A hiding agent, movable box, and wall can all be entities.</p>
<p>This separation avoids pointer-heavy object graphs. Identity remains stable while data is stored in structures optimized for iteration.</p>
<h2>Component: data in columns</h2>
<p>Components hold facts such as position, rotation, velocity, action, team, or reward state. Instead of storing every property together inside one object, the engine stores components in columns. A system reading positions can traverse contiguous position data.</p>
<p>Madrona combines component rows from many worlds in GPU memory and records which world each row belongs to. This produces a large data-parallel table even when individual worlds are small and different.</p>
<h2>System: behavior as a query</h2>
<p>A system declares the components it needs. An action-processing system asks for entities with position and action. Collision asks for position and bounds. Reward computation asks for the state relevant to the task.</p>
<p>The engine finds matching rows and maps invocations to GPU threads. Because behavior depends on component presence rather than a virtual class hierarchy, the engine gets a form of runtime polymorphism without expensive per-thread virtual dispatch.</p>
<h2>Procedural generation and variable world sizes</h2>
<p>Hide-and-Seek worlds can contain different obstacles and layouts. Fixed tensors would require padding every world to the largest possible count or complex masking. ECS tables simply contain the entities that exist.</p>
<p>The developer still uses a direct interface such as creating an obstacle. Madrona translates those operations into batched table management. This programming model is a central contribution: performance would be less useful if every environment required hand-written allocation kernels.</p>
<h2>Dynamic allocation through append and compact</h2>
<p>Immediate fine-grained allocation is difficult on a GPU. Madrona exploits throughput tolerance. New rows are appended. Deleted rows are marked rather than immediately removed. Later, a high-performance GPU sort or compaction pass reorganizes the tables.</p>
<p>This resembles garbage collection: defer cleanup, gather enough work, then process it efficiently in bulk. One world may temporarily carry dead rows, but the entire batch gains throughput.</p>
<h2>The task graph for a frame</h2>
<p>A complete environment step contains dependencies. Actions must be available before movement. Physics updates positions before observations. Rewards depend on resulting state. Madrona expresses these systems as a task graph.</p>
<p>The graph tells the runtime which work can execute and where synchronization is required. Developers reason about environment stages while the engine schedules large batches of system invocations.</p>
<h2>Persistent mega-kernel scheduling</h2>
<p>Launching a separate GPU kernel for every tiny system would add overhead and repeatedly return control to the CPU. Madrona uses a persistent mega-kernel that remains active and pulls ready tasks from the graph.</p>
<p>Fast GPU atomics coordinate workers. SMs can take different systems as dependencies clear. Brennan’s visualization shows colored activity across SMs and narrow synchronization gaps, indicating that the runtime keeps the device busy.</p>
<h2>Reading the utilization result</h2>
<p>The demonstration uses roughly 4,000 Hide-and-Seek worlds on an RTX 4090. Brennan says synchronization consumes under one percent of the shown work. This supports the scheduling claim: irregular gameplay does not automatically force the GPU to sit idle.</p>
<p>Utilization alone is not success. A kernel can keep hardware busy doing unnecessary work. The environment-throughput comparison is needed to show useful progress.</p>
<h2>The CPU baselines</h2>
<p>The team chose environments with existing CPU implementations, including Hide-and-Seek and Overcooked. The original machine-learning environments provide one baseline. A CPU implementation using the same ECS organization provides another, separating the benefit of data-oriented design from GPU execution.</p>
<p>Brennan reports that the complete GPU version is over 100× faster in many shown cases. “Many cases” matters: environment complexity and baseline quality change the factor. The claim should not be generalized to every game engine.</p>
<h2>From simulator speed to end-to-end training</h2>
<p>A simulator benchmark can produce frames faster than a policy consumes them. Later projects tested the complete loop, including policy inference and learning. Brennan says the architecture extended successfully beyond isolated simulation throughput.</p>
<p>Keeping observations, actions, rewards, and model computation on the GPU can avoid CPU transfers between every step. The exact training architecture determines whether simulation, inference, or optimization becomes the next bottleneck.</p>
<h2>Why accessibility to ML researchers matters</h2>
<p>A framework succeeds only if researchers can create environments without becoming GPU-runtime specialists. Brennan reports that users with little low-level GPU knowledge built environments and obtained strong performance.</p>
<p>The ECS interface, task graph, and runtime absorb complexity. This is analogous to a tensor framework: most users describe computation, while experts optimize execution beneath the abstraction.</p>
<h2>What Python-like syntax does not solve</h2>
<p>Many GPU languages make CUDA concepts easier to write by adopting Python syntax. Brennan appreciates that improvement but argues it leaves the underlying model unchanged. Dynamic allocation, irregular entities, and runtime polymorphism still require architectural support.</p>
<p>A true high-level GPU environment should provide data structures and scheduling suited to irregular workloads, not merely translate a thread kernel into prettier syntax.</p>
<h2>GPU compute beyond tensor cores</h2>
<p>Modern GPUs contain massive bandwidth, general arithmetic units, atomics, and scheduling capacity in addition to tensor cores. Workloads such as simulation, graph processing, and dynamic systems may exploit these resources without looking like matrix multiplication.</p>
<p>Madrona demonstrates that the obstacle is often the programming model. When data and scheduling are reorganized, branchy game logic can become a productive throughput workload.</p>
<h2>What Madrona does not automatically guarantee</h2>
<p>High simulation speed does not prove that a simulated world matches reality. A policy can learn artifacts of the environment. Reward design can be wrong. Physics fidelity may be insufficient for a target task. Those questions belong to the research using the engine.</p>
<p>Madrona is infrastructure for generating experience efficiently. Researchers still need validation, randomization, and evaluation appropriate to their domain.</p>
<h2>A practical environment-building workflow</h2>
<ol><li>Define entities and the minimum component data each requires.</li><li>Write systems as queries over components.</li><li>Express frame dependencies in the task graph.</li><li>Batch many worlds and vary their layouts procedurally.</li><li>Keep observations, actions, and rewards in GPU-accessible structures.</li><li>Profile synchronization, allocation, and per-system utilization.</li><li>Compare against the same environment on a strong CPU baseline.</li><li>Validate learning quality, not only steps per second.</li></ol>
<h2>When a GPU-native engine is the wrong tool</h2>
<p>If an experiment needs only a few worlds, strict per-world latency, or an existing complex commercial engine, rewriting everything around ECS tables may not repay the effort. CPU engines have mature tooling and debugging ecosystems.</p>
<p>The approach is most compelling when environment throughput is a measured bottleneck, many independent worlds exist, and the research program will run enough experiments to amortize the port.</p>
<h2>The broader systems lesson</h2>
<p>Madrona specializes the software architecture around the workload rather than merely adding a faster device. It changes storage, allocation, dispatch, synchronization, and the definition of a frame. That is why it belongs in this Edition 03 sequence.</p>
<p>The final insight is transferable: a GPU is not limited to conventional tensor operations, but irregular software must be reorganized around bulk data and throughput. Good abstractions make that reorganization available to people who should be thinking about environments and learning—not atomic instructions.</p>`
};
